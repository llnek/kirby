/*Auto generated by Kirby - v1.0.0 czlab.kirby.compiler Fri Dec 15 2017 22:53:24 GMT-0800 (PST)*/

const smap = require("source-map");
const std = require("./stdlib");
const lambda_DASH_arg_QUERY = std["lambda_DASH_arg_QUERY"];
const hashmap = std["hashmap"];
const pairs_QUERY = std["pairs_QUERY"];
const into_BANG = std["into_BANG"];
const contains_QUERY = std["contains_QUERY"];
const last = std["last"];
const typeid = std["typeid"];
const prn = std["prn"];
const primitive = std["primitive"];
const map_QUERY = std["map_QUERY"];
const vector_QUERY = std["vector_QUERY"];
const opt_QUERY__QUERY = std["opt_QUERY__QUERY"];
const some_QUERY = std["some_QUERY"];
const primitive_QUERY = std["primitive_QUERY"];
const not_DASH_empty = std["not_DASH_empty"];
const conj_BANG = std["conj_BANG"];
const list_QUERY = std["list_QUERY"];
const gensym = std["gensym"];
const count = std["count"];
const pop_BANG = std["pop_BANG"];
const slice = std["slice"];
const symbol = std["symbol"];
const keyword = std["keyword"];
const symbol_QUERY = std["symbol_QUERY"];
const keyword_QUERY = std["keyword_QUERY"];
const eng = require("./engine");
const parser = require("./parser");
const jsid = parser["jsid"];
const fs = require("fs");
const path = require("path");
const esfmt = require("esformatter");
const kirbystdlibref = std;
const ERRORS_DASH_MAP = {
  "no-sourcemap-info": "Expected source map info",
  "invalid-fargs": "Invalid function args",
  "invalid-catch": "Invalid catch clause",
  "invalid-try": "Invalid try clause",
  "invalid-include": "Invalid include clause",
  "invalid-require": "Invalid require clause",
  "invalid-namespace": "Invalid namespace clause",
  "destruct-args": "Bad destructure args",
  "outside-macro": "Invalid clause outside of macro",
  "file-access": "Failed file access",
  "file-read": "Failed file read",
  "file-open": "Failed to open file",
  "unknown-keyword": "Unknown keyword",
  "invalid-arity": "Invalid function arity",
  "invalid-meta": "Invalid meta data",
  "syntax-error": "Syntax error",
  "empty-form": "Invalid form (empty)"
};
const ARRSLICE = "Array.prototype.slice.call";
const JSARGS = "arguments";
const LARGS = "____args";
const BREAK = "____break";
const MODULE_DASH_VERSION = "1.0.0";
var _STAR_externs_STAR = null;
var SPEC_DASH_OPS = {};
var MATH_DASH_OP_DASH_REGEX = /^[-+][0-9]+$/;
////////////////////////////////////////////////////////////////////////////////
//name: [tnodeEx] in file: compiler.ky near line: 60
//Create a node
const tnodeEx = function(name) {
  let G____8 = Array.prototype.slice.call(arguments, 1);
  let chunk = G____8[0];
  return tnode(null, null, null, chunk, name);
};
////////////////////////////////////////////////////////////////////////////////
//name: [tnode] in file: compiler.ky near line: 64
//Create a token
//with source information
const tnode = function() {
  let G____9 = Array.prototype.slice.call(arguments, 0);
  let source = G____9[0];
  let line = G____9[1];
  let col = G____9[2];
  let chunk = G____9[3];
  let name = G____9[4];
  return new smap.SourceNode(opt_QUERY__QUERY(line, null), opt_QUERY__QUERY(col, null), opt_QUERY__QUERY(source, null), opt_QUERY__QUERY(chunk, null), opt_QUERY__QUERY(name, null));
};
////////////////////////////////////////////////////////////////////////////////
//name: [doFuncArgs] in file: compiler.ky near line: 74
//Deal with possible destructuring
//of args in function definition
const doFuncArgs = function(args, env) {
  let e,
    ev,
    rval,
    out;
  let pms = [];
  let ret = [node_QTE(args), node_QTE(args)];
  let fargs = ret[0];
  let fdefs = ret[1];
  for (let i = 0, sz = kirbystdlibref.count(args), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    e = args[i];
    if (symbol_QUERY(e)) {
      if ( (e == "&") ) {
        (rval = node_QTE(args), e = args[i + 1]);
        rval.add([ARRSLICE, "(", JSARGS, ",", [i].join(""), ")"]);
        if (symbol_QUERY(e)) {
          fdefs.add(["let ", tx_STAR(e, env), "=", rval, ";\n"]);
        } else {
          (out = node_QTE(args), e = destruct_STAR(e, out, env));
          fdefs.add(["let ", tx_STAR(e, env), "=", rval, ";\n", out]);
        }
        ____break = true;
      } else {
        if ( (e == "_") ) {
          e = xfi(e, gensym("U__"));
        }
        conj_BANG(pms, e);
      }
    } else {
      if (Array.isArray(e)) {
        (rval = node_QTE(args), out = node_QTE(args), e = destruct_STAR(e, out, env));
        rval.add([JSARGS, "[", [i].join(""), "]"]);
        conj_BANG(pms, e);
        fdefs.add(out);
      } else {
        if (true) {
          error_BANG("destruct-args", args);
        }
      }
    }
  }
  pms.forEach(function() {
    let ____args = Array.prototype.slice.call(arguments);
    return fargs.add(tx_STAR(____args[0], env));
  });
  fargs.join(",");
  return ret;
};
////////////////////////////////////////////////////////////////////////////////
//name: [destruct*] in file: compiler.ky near line: 116
//Decide on what the
//rhs should be referred to 'as'
const destruct_STAR = function(coll, out, env) {
  let rhs = xfi(coll, gensym());
  let e = null;
  for (let i = 0, sz = kirbystdlibref.count(coll), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    e = coll[i];
    if ( (keyword_QUERY(e) && (e == "as")) ) {
      rhs = std.keyword_DASH__GT_symbol(coll[i + 1]);
      ____break = true;
    }
  }
  if (map_QUERY(coll)) {
    out.add(destructMap(rhs, coll, env));
  } else {
    if (vector_QUERY(coll)) {
      out.add(destructVec(rhs, coll, env));
    } else {
      null;
    }
  }
  return rhs;
};
////////////////////////////////////////////////////////////////////////////////
//name: [destructVec] in file: compiler.ky near line: 135
const destructVec = function(src, coll, env) {
  let ret = node_QTE(coll);
  let as = tx_STAR(src, env);
  let e,
    rval,
    out;
  for (let i = 0, sz = kirbystdlibref.count(coll), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    e = coll[i];
    if (symbol_QUERY(e)) {
      if ( (e == "_") ) {
        null;
      } else {
        if ( (e == "&") ) {
          (rval = node_QTE(coll), out = node_QTE(coll), e = coll[i + 1]);
          rval.add([ARRSLICE, "(", as, ",", [i].join(""), ")"]);
          if ( (!symbol_QUERY(e)) ) {
            e = destruct_STAR(e, out, env);
          }
          ret.add(["let ", tx_STAR(e, env), "=", rval, ";\n", out]);
          ____break = true;
        } else {
          if (true) {
            ret.add(["let ", tx_STAR(e, env), "=", as, "[", [i].join(""), "];\n"]);
          }
        }
      }
    } else {
      if (Array.isArray(e)) {
        (rval = node_QTE(coll), out = node_QTE(coll), e = destruct_STAR(e, out, env));
        rval.add([as, "[", [i].join(""), "]"]);
        ret.add(["let ", tx_STAR(e, env), "=", rval, ";\n", out]);
      } else {
        if (keyword_QUERY(e)) {
          if ( (e == "as") ) {
            ++i;
          } else {
            error_BANG("unknown-keyword", coll);
          }
        } else {
          if (true) {
            error_BANG("syntax-error", coll);
          }
        }
      }
    }
  }
  return ret;
};
////////////////////////////////////////////////////////////////////////////////
//name: [destructMap] in file: compiler.ky near line: 176
const destructMap = function(src, coll, env) {
  let ret = node_QTE(coll);
  let as = tx_STAR(src, env);
  let e,
    arr;
  for (let i = 0, sz = kirbystdlibref.count(coll), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    e = coll[i];
    if (keyword_QUERY(e)) {
      if ( ((e == "keys") || (e == "strs")) ) {
        arr = coll[i + 1];
        ++i;
        for (let j = 0, sz = kirbystdlibref.count(arr), ____break = false; ((!____break) && (j < sz)); j = (j + 1)) {
          e = arr[j];
          ret.add(["let ", tx_STAR(e, env), "=", as, "[\"", [e].join(""), "\"];\n"]);
        }
      } else {
        if (true) {
          error_BANG("unknown-keyword", coll);
        }
      }
    } else {
      if (true) {
        error_BANG("syntax-error", coll);
      }
    }
  }
  return ret;
};
////////////////////////////////////////////////////////////////////////////////
//name: [simple?] in file: compiler.ky near line: 202
const simple_QUERY = function(ast) {
  return ((typeof (ast) === "undefined") || (ast === null) || (typeof (ast) === "string") || (typeof (ast) === "number") || (typeof (ast) === "boolean"));
};
////////////////////////////////////////////////////////////////////////////////
//name: [wrap] in file: compiler.ky near line: 206
const wrap = function(ret, head, tail) {
  if (head) {
    ret.prepend(head);
  }
  if (tail) {
    ret.add(tail);
  }
  return ret;
};
////////////////////////////////////////////////////////////////////////////////
//name: [exprHint] in file: compiler.ky near line: 211
const exprHint = function(ast, flag) {
  if (simple_QUERY(ast)) {
    ast = primitive(ast);
  }
  ast["____expr"] = flag;
  return ast;
};
////////////////////////////////////////////////////////////////////////////////
//name: [stmt?] in file: compiler.ky near line: 215
const stmt_QUERY = function(ast) {
  if (simple_QUERY(ast)) {
    error_BANG("syntax-error", ast);
  }
  return (false === ast.____expr);
};
////////////////////////////////////////////////////////////////////////////////
//name: [error!] in file: compiler.ky near line: 219
const error_BANG = function(e) {
  let G____11 = Array.prototype.slice.call(arguments, 1);
  let ast = G____11[0];
  let msg = G____11[1];
  return (function() {
    throw new Error([ERRORS_DASH_MAP[e],(msg ?
      [" : ", msg].join("") :
      null),((ast && ast.line) ?
      ["\nline: ", ast.line].join("") :
      null),((ast && ast.source) ?
      ["\nfile: ", ast.source].join("") :
      null)].join("")) ;
  }).call(this);
};
////////////////////////////////////////////////////////////////////////////////
//name: [testre?] in file: compiler.ky near line: 226
const testre_QUERY = function(re, x) {
  return (x ?
    re.test(x) :
    false);
};
////////////////////////////////////////////////////////////////////////////////
//name: [pad] in file: compiler.ky near line: 229
const pad = function(n) {
  return " ".repeat(n);
};
////////////////////////////////////////////////////////////////////////////////
//name: [node'] in file: compiler.ky near line: 232
const node_QTE = function(ast) {
  let G____12 = Array.prototype.slice.call(arguments, 1);
  let obj = G____12[0];
  obj = opt_QUERY__QUERY(obj, tnode());
  (obj["source"] = ast.source, obj["line"] = ast.line, obj["column"] = ast.column);
  return obj;
};
////////////////////////////////////////////////////////////////////////////////
//name: [txTree] in file: compiler.ky near line: 239
const txTree = function(root, env) {
  let ret = node_QTE(root);
  let G__1 = root;
  for (let G__3 = 0, G__2 = false, ____break = false; ((!____break) && ((!G__2) && (G__3 < G__1.length))); G__3 = (G__3 + 1)) {
    let r = G__1[G__3];
    let t = tx_STAR(r, env);
    if ( (!true) ) {
      G__2 = true;
    }
    if ( ((!G__2) && some_QUERY(t)) ) {
      ret.add([t, "\n"]);
    }
  }
  null;
  return ret;
};
////////////////////////////////////////////////////////////////////////////////
//name: [txForm] in file: compiler.ky near line: 246
const txForm = function(expr, env) {
  if (Array.isArray(expr)) {
    expr.forEach(function(a, i, arr) {
      return arr[i] = tx_STAR(a, env);
    });
  }
  return expr;
};
////////////////////////////////////////////////////////////////////////////////
//name: [txAtom] in file: compiler.ky near line: 252
//Returns a SourceNode or String
const txAtom = function(a) {
  let s = [a].join("");
  return (lambda_DASH_arg_QUERY(a) ?
    [LARGS, "[", s, "]"].join("") :
    (keyword_QUERY(a) ?
      node_QTE(a, tnodeEx(jsid(s), ["\"", jsid(s), "\""].join(""))) :
      (symbol_QUERY(a) ?
        node_QTE(a, tnodeEx(jsid(s), jsid(s))) :
        ((a === null) ?
          "null" :
          (primitive_QUERY(a) ?
            (function() {
              (a = a.value, s = [a].join(""));
              return ((typeof (a) === "string") ?
                std.wrap_DASH_str(a) :
                ((a === null) ?
                  "null" :
                  (true ?
                    s :
                    null)));
            }).call(this) :
            ((typeof (a) === "string") ?
              std.wrap_DASH_str(a) :
              (true ?
                jsid(s) :
                null)))))));
};
////////////////////////////////////////////////////////////////////////////////
//name: [tx*] in file: compiler.ky near line: 272
const tx_STAR = function(x) {
  let G____13 = Array.prototype.slice.call(arguments, 1);
  let env = G____13[0];
  return (Array.isArray(x) ?
    txPairs(x, env) :
    txAtom(x));
};
////////////////////////////////////////////////////////////////////////////////
//name: [gcmd] in file: compiler.ky near line: 276
const gcmd = function(ast) {
  return (map_QUERY(ast) ?
    "hashmap" :
    (vector_QUERY(ast) ?
      "vec" :
      (list_QUERY(ast) ?
        "list" :
        ((Array.isArray(ast) && (!Array.isArray(ast[0]))) ?
          [ast[0]].join("") :
          (true ?
            "" :
            null)))));
};
////////////////////////////////////////////////////////////////////////////////
//name: [quoteSingle] in file: compiler.ky near line: 284
const quoteSingle = function(a) {
  return (keyword_QUERY(a) ?
    [maybeStripStdlib("kirbystdlibref.keyword"), "(\":", a, "\")"].join("") :
    (symbol_QUERY(a) ?
      [maybeStripStdlib("kirbystdlibref.symbol"), "(\"", a, "\")"].join("") :
      (primitive_QUERY(a) ?
        (function() {
          a = a.value;
          return ((typeof (a) === "string") ?
            std.wrap_DASH_str(a) :
            ((a === null) ?
              "null" :
              (true ?
                [a].join("") :
                null)));
        }).call(this) :
        ((typeof (a) === "string") ?
          std.wrap_DASH_str(a) :
          (true ?
            [a].join("") :
            null)))));
};
////////////////////////////////////////////////////////////////////////////////
//name: [quote!] in file: compiler.ky near line: 301
const quote_BANG = function(ast, env) {
  assertInfo(ast);
  return (Array.isArray(ast) ?
    (map_QUERY(ast) ?
      quoteMap(ast, env) :
      quoteBlock(ast, env)) :
    quoteSingle(ast));
};
////////////////////////////////////////////////////////////////////////////////
//name: [quoteMap] in file: compiler.ky near line: 310
const quoteMap = function(ast, env) {
  let comma = "";
  let ret = node_QTE(ast);
  for (let i = 0, sz = kirbystdlibref.count(ast), ____break = false; ((!____break) && (i < sz)); i = (i + 2)) {
    if ( (i > 0) ) {
      ret.add(",");
    }
    ret.add([quote_BANG(ast[i], env), " : ", quote_BANG(ast[i + 1], env)]);
  }
  if ( (!(0 === kirbystdlibref.count(ast))) ) {
    comma = ",";
  }
  return wrap(ret, ["[", "symbol(\"hashmap\")", comma], "]");
};
////////////////////////////////////////////////////////////////////////////////
//name: [quoteBlock] in file: compiler.ky near line: 322
const quoteBlock = function(ast, env) {
  let ret = node_QTE(ast);
  for (let i = 0, sz = kirbystdlibref.count(ast), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    if ( (i > 0) ) {
      ret.add(",");
    }
    ret.add(quote_BANG(ast[i], env));
  }
  return wrap(ret, "[", "]");
};
////////////////////////////////////////////////////////////////////////////////
//name: [spreadInfo] in file: compiler.ky near line: 331
const spreadInfo = function(from, to) {
  return ((from && (!simple_QUERY(from)) && (typeof (from.line) === "number") && Array.isArray(to)) ?
    (function() {
      xfi(from, to);
      return (function() {
        for (let i = 0, sz = kirbystdlibref.count(to), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
          spreadInfo(from, to[i]);
        }
      }).call(this);
    }).call(this) :
    xfi(from, to));
};
////////////////////////////////////////////////////////////////////////////////
//name: [txPairs] in file: compiler.ky near line: 343
const txPairs = function(ast, env) {
  let nsp = eng.globalEnv().peekNSP();
  let stmtQ = stmt_QUERY(ast);
  let ret = node_QTE(ast);
  let cmd = gcmd(ast);
  let e1 = ast[0];
  let orig = ast;
  let op = null;
  let tmp = null;
  let mc = eng.getMacro(cmd);
  xfi(e1, ret);
  xfi(e1, ast);
  if (mc) {
    (ast = eng.expandMacro(ast, env, mc), ast = xfi(orig, exprHint(ast, (!stmtQ))));
    spreadInfo(orig, ast);
    cmd = gcmd(ast);
  }
  if (parser.REGEX.int.test(cmd)) {
    if ( (!(cmd.startsWith("+") || cmd.startsWith("-"))) ) {
      cmd = ["+", cmd].join("");
    }
    (ast = xfi(ast, [symbol(cmd.charAt(0)), ast[1], parseInt(cmd.slice(1))]), cmd = [ast[0]].join(""));
  }
  op = SPEC_DASH_OPS[cmd];
  if ( (cmd == "with-meta") ) {
    ret.add(tx_STAR(meta_QUERY__QUERY(ast, env)[1], env));
  } else {
    if (cmd.startsWith(".-")) {
      ret.add([tx_STAR(ast[1], env), ".", tx_STAR(symbol(cmd.slice(2)), env)]);
    } else {
      if (cmd.startsWith(".@")) {
        tmp = (cmd.startsWith(".@+") ?
          [3, true] :
          [2, false]);
        ret.add([tx_STAR(ast[1], env), "[", cmd.slice(tmp[0]),(tmp[1] ?
          "+1]" :
          "]")]);
      } else {
        if (cmd.startsWith(".")) {
          ret.add([tx_STAR(ast[1], env), cmd, "("]);
          for (let n = 2, sz = kirbystdlibref.count(ast), ____break = false; ((!____break) && (n < sz)); n = (n + 1)) {
            if ( (n !== 2) ) {
              ret.add(",");
            }
            ret.add(tx_STAR(ast[n], env));
          }
          ret.add(")");
        } else {
          if (some_QUERY(op)) {
            ret = op(ast, env);
          } else {
            if ( (((cmd == "splice-unquote") || (cmd == "unquote") || (cmd == "quasiquote")) && (!nsp.startsWith("czlab.kirby."))) ) {
              error_BANG("outside-macro", ast);
            } else {
              if (true) {
                if (pairs_QUERY(ast)) {
                  txForm(ast, env);
                  cmd = [ast[0]].join("");
                } else {
                  cmd = tx_STAR(ast, env);
                }
                if ( (!cmd) ) {
                  error_BANG("empty-form", ast);
                }
                cmd = maybeStripStdlib(cmd);
                if (pairs_QUERY(ast)) {
                  ret.add([(testre_QUERY(parser.REGEX.func, cmd) ?
                    ["(", cmd, ")"].join("") :
                    cmd), "(", ast.slice(1).join(","), ")"]);
                } else {
                  ret.add(cmd);
                }
              }
            }
          }
        }
      }
    }
  }
  return node_QTE(ast, ret);
};
////////////////////////////////////////////////////////////////////////////////
//name: [writeDoc] in file: compiler.ky near line: 412
//Convert to jsdoc
const writeDoc = function(doc) {
  let out = [];
  if (doc) {
    std.unwrap_DASH_str(doc).split("\n").forEach(function() {
      let ____args = Array.prototype.slice.call(arguments);
      return (function() {
        let s = [____args[0]].join("").trim();
        return (not_DASH_empty(s) ?
          conj_BANG(out, ["//", s, "\n"].join("")) :
          null);
      }).call(this);
    });
  }
  return out;
};
////////////////////////////////////////////////////////////////////////////////
//name: [txDo] in file: compiler.ky near line: 421
const txDo = function(ast, env) {
  let G____14 = Array.prototype.slice.call(arguments, 2);
  let return_QUERY = G____14[0];
  let stmtQ = stmt_QUERY(ast);
  let ret = node_QTE(ast);
  let e = null;
  let end = (ast.length - 1);
  return_QUERY = (stmtQ ?
    false :
    opt_QUERY__QUERY(return_QUERY, true));
  for (let i = 0, ____break = false; ((!____break) && (i < end)); i = (i + 1)) {
    e = exprHint(ast[i], false);
    ret.add([tx_STAR(e, env), ";\n"]);
  }
  if ( (end >= 0) ) {
    (e = exprHint(ast[end], (!stmtQ)), e = tx_STAR(e, env));
    ret.add(((!return_QUERY) ?
      [e, ";\n"] :
      ["return ", e, ";\n"]));
  }
  return ret;
};
////////////////////////////////////////////////////////////////////////////////
//name: [meta??] in file: compiler.ky near line: 439
const meta_QUERY__QUERY = function(obj, env) {
  let mobj = null;
  if ( (Array.isArray(obj) && (3 === kirbystdlibref.count(obj)) && ("with-meta" == [obj[0]].join(""))) ) {
    mobj = evalMeta(obj[2], env);
    obj[1]["____meta"] = mobj;
    obj = obj[1];
  }
  return [mobj, obj];
};
////////////////////////////////////////////////////////////////////////////////
//name: [fmtRegoSpecOps] in file: compiler.ky near line: 451
const fmtRegoSpecOps = function(alias, fname) {
  return [parser.jsid("SPEC-OPS"), "[\"", alias, "\"] = ", fname].join("");
};
////////////////////////////////////////////////////////////////////////////////
//name: [fmtSpecOps] in file: compiler.ky near line: 455
const fmtSpecOps = function(fname, attrs) {
  let ks = (attrs["opcode"] || []);
  let out = ks.map(function() {
    let ____args = Array.prototype.slice.call(arguments);
    return fmtRegoSpecOps([____args[0]].join(""), fname);
  }).join(";\n");
  return (not_DASH_empty(ks) ?
    [out, ";\n"].join("") :
    out);
};
////////////////////////////////////////////////////////////////////////////////
//name: [writeFuncInfo] in file: compiler.ky near line: 462
const writeFuncInfo = function(fname, ast) {
  let file = (ast.source ?
    ast.source.slice((ast.source.lastIndexOf("/") + 1)) :
    "?");
  let s = ["//fn: [", fname, "] in file: ", file, ",line: ",(ast.line || "?"), "\n"].join("");
  let len = s.length;
  if ( (len < 80) ) {
    len = 80;
  }
  return ["/".repeat(len), "\n", s].join("");
};
var icache = [];
////////////////////////////////////////////////////////////////////////////////
//name: [includeFile] in file: compiler.ky near line: 477
const includeFile = function(fname, ast) {
  return (contains_QUERY(icache, fname) ?
    node_QTE(ast) :
    (function() {
      let src = eng.slurp(fname);
      conj_BANG(icache, fname);
      return txTree(parser.parse(src, fname), eng.globalEnv());
    }).call(this));
};
////////////////////////////////////////////////////////////////////////////////
//name: [evalMeta] in file: compiler.ky near line: 485
const evalMeta = function(ast, env) {
  let x = (Array.isArray(ast) ?
    ast :
    (keyword_QUERY(ast) ?
      into_BANG("map", [ast, true]) :
      (symbol_QUERY(ast) ?
        into_BANG("map", [keyword(":tag"), ast]) :
        (true ?
          error_BANG("invalid-meta", ast) :
          null))));
  let v = eng.compute(x, env);
  return v;
};
////////////////////////////////////////////////////////////////////////////////
//name: [maybeStripStdlib] in file: compiler.ky near line: 500
const maybeStripStdlib = function(cmd) {
  let lib = "kirbystdlibref.";
  let nsp = eng.globalEnv().peekNSP();
  cmd = [cmd].join("");
  return ((cmd.startsWith(lib) && (nsp === "czlab.kirby.stdlib")) ?
    cmd.slice(lib.length) :
    cmd);
};
////////////////////////////////////////////////////////////////////////////////
//name: [assertArity] in file: compiler.ky near line: 509
const assertArity = function(kond, ast) {
  if ( (!kond) ) {
    error_BANG("invalid-arity", ast);
  }
  return assertInfo(ast);
};
////////////////////////////////////////////////////////////////////////////////
//name: [assertInfo] in file: compiler.ky near line: 514
const assertInfo = function(ast) {
  return ((false && ast && (!simple_QUERY(ast)) && (!(typeof (ast.line) === "number"))) ?
    error_BANG("no-sourcemap-info", ast) :
    null);
};
////////////////////////////////////////////////////////////////////////////////
//name: [sf-juxt] in file: compiler.ky near line: 524
const sf_DASH_juxt = function(ast, env) {
  let ret = node_QTE(ast);
  ret.add(["function () {\n", "let ret=[],", LARGS, "=", ARRSLICE, "(", JSARGS, ");\n"]);
  for (let i = 1, f = null, sz = kirbystdlibref.count(ast), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    f = [gensym("F__")].join("");
    ret.add(["let ", f, "=", tx_STAR(ast[i], env), ";\n", "ret.push(", f, ".apply(this,____args));\n"]);
  }
  ret.add("return ret;\n}");
  return ret;
};
SPEC_DASH_OPS["juxt"] = sf_DASH_juxt;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-compose] in file: compiler.ky near line: 536
const sf_DASH_compose = function(ast, env) {
  assertArity((kirbystdlibref.count(ast) >= 2), ast);
  let ret = node_QTE(ast);
  let end = (ast.length - 1);
  let f,
    r,
    prev;
  ret.add(["function () {\n"]);
  ret.add(["let ", LARGS, "=", ARRSLICE, "(", JSARGS, ");\n"]);
  for (let i = end, ____break = false; ((!____break) && (i > 0)); i = (i - 1)) {
    (f = [gensym("F__")].join(""), r = [gensym("R__")].join(""));
    ret.add(["let ", f, "=", tx_STAR(ast[i], env), ";\n"]);
    if ( (i === end) ) {
      ret.add(["let ", r, "=", f, ".apply(this,", LARGS, ");\n"]);
    } else {
      ret.add(["let ", r, "=", f, "(", prev, ");\n"]);
    }
    prev = r;
  }
  ret.add(["return ", prev, ";\n"]);
  return ret.add("}");
};
SPEC_DASH_OPS["comp"] = sf_DASH_compose;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-quote] in file: compiler.ky near line: 555
const sf_DASH_quote = function(ast, env) {
  assertArity((kirbystdlibref.count(ast) === 2), ast);
  let ret = node_QTE(ast);
  return wrap(ret, null, quote_BANG(ast[1], env));
};
SPEC_DASH_OPS["quote"] = sf_DASH_quote;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-deftype] in file: compiler.ky near line: 562
//Define a JS Class
const sf_DASH_deftype = function(ast, env) {
  assertArity((kirbystdlibref.count(ast) >= 4), ast);
  let ret = node_QTE(ast);
  let mtd,
    pos,
    n,
    m;
  let private_QUERY = [ast[0]].join("").endsWith("-");
  let czname = tx_STAR(ast[1], env);
  let par = ast[2][0];
  let czargs = ast[3];
  let G____15 = ((typeof (ast[4]) === "string") ?
    [ast[4], Array.prototype.slice.call(ast, 5)] :
    [null, Array.prototype.slice.call(ast, 4)]);
  let doc = G____15[0];
  let mtds = G____15[1];
  ret.add(["class ", czname]);
  if (par) {
    ret.add([" extends ", tx_STAR(par, env)]);
  }
  ret.add(" {\n");
  let G__4 = mtds;
  for (let G__6 = 0, G__5 = false, ____break = false; ((!____break) && ((!G__5) && (G__6 < G__4.length))); G__6 = (G__6 + 1)) {
    let m = G__4[G__6];
    null;
    if ( (!true) ) {
      G__5 = true;
    }
    if ( ((!G__5) && true) ) {
      (mtd = kirbystdlibref.symbol("method"), pos = ((typeof (m[1]) === "string") ?
        3 :
        2));
      xfi(m[0], mtd);
      xfi(m[0], m);
      if ( (([m[0]].join("") == "constructor") && not_DASH_empty(czargs)) ) {
        for (let x = 0, sz = kirbystdlibref.count(czargs), ____break = false; ((!____break) && (x < sz)); x = (x + 2)) {
          czargs[x] = [czargs[x]].join("");
        }
        m.splice(pos, 0, [kirbystdlibref.symbol("set-in!"), kirbystdlibref.symbol("this")].concat(czargs));
      }
      m.unshift(mtd);
      ret.add([sf_DASH_func(m, env, false), "\n"]);
    }
  }
  null;
  ret.add("}\n");
  if (doc) {
    ret.prepend(writeDoc(doc));
  }
  if ( ((!private_QUERY) && (1 === eng.globalEnv().countNSPCache())) ) {
    _STAR_externs_STAR[czname] = czname;
  }
  return ret;
};
SPEC_DASH_OPS["deftype"] = sf_DASH_deftype;
SPEC_DASH_OPS["deftype-"] = sf_DASH_deftype;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-compOp] in file: compiler.ky near line: 604
const sf_DASH_compOp = function(ast, env) {
  assertArity(((kirbystdlibref.count(ast) >= 3) && (!(0 === kirbystdlibref.modulo(kirbystdlibref.count(ast), 2)))), ast);
  let ret = node_QTE(ast);
  let cmd = [ast[0]].join("");
  if ( (cmd == "not=") ) {
    ast[0] = kirbystdlibref.symbol("!==");
  } else {
    if ( (cmd == "=") ) {
      ast[0] = kirbystdlibref.symbol("===");
    } else {
      null;
    }
  }
  for (let i = 1, op = tx_STAR(ast[0], env), ____break = false; ((!____break) && (i < (ast.length - 1))); i = (i + 1)) {
    if ( (i !== 1) ) {
      ret.add(" && ");
    }
    ret.add([tx_STAR(ast[i], env), " ", op, " ", tx_STAR(ast[i + 1], env)]);
  }
  return wrap(ret, "(", ")");
};
SPEC_DASH_OPS["not="] = sf_DASH_compOp;
SPEC_DASH_OPS["!="] = sf_DASH_compOp;
SPEC_DASH_OPS["=="] = sf_DASH_compOp;
SPEC_DASH_OPS["="] = sf_DASH_compOp;
SPEC_DASH_OPS[">"] = sf_DASH_compOp;
SPEC_DASH_OPS[">="] = sf_DASH_compOp;
SPEC_DASH_OPS["<"] = sf_DASH_compOp;
SPEC_DASH_OPS["<="] = sf_DASH_compOp;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-arithOp] in file: compiler.ky near line: 622
const sf_DASH_arithOp = function(ast, env) {
  assertArity((kirbystdlibref.count(ast) >= 2), ast);
  let e1 = tx_STAR(ast[0], env);
  let cmd = [e1].join("");
  let ret = node_QTE(ast);
  if ( (cmd == "bitwise-zrshift") ) {
    cmd = ">>>";
  } else {
    if ( (cmd == "bitwise-rshift") ) {
      cmd = ">>";
    } else {
      if ( (cmd == "bitwise-lshift") ) {
        cmd = "<<";
      } else {
        if ( (cmd == "bitwise-and") ) {
          cmd = "&";
        } else {
          if ( (cmd == "bitwise-or") ) {
            cmd = "|";
          } else {
            if ( (cmd == "bitwise-not") ) {
              cmd = "~";
            } else {
              if ( (cmd == "bitwise-xor") ) {
                cmd = "^";
              } else {
                if ( (cmd == "rem") ) {
                  cmd = "%";
                } else {
                  if ( (cmd == "div") ) {
                    cmd = "/";
                  } else {
                    if ( (cmd == "and") ) {
                      cmd = "&&";
                    } else {
                      if ( (cmd == "or") ) {
                        cmd = "||";
                      } else {
                        if ( (cmd == "exp") ) {
                          cmd = "**";
                        } else {
                          null;
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
  if ( (("-" == cmd) && (2 === kirbystdlibref.count(ast))) ) {
    ret.add("-1 * ");
  }
  for (let i = 1, sz = kirbystdlibref.count(ast), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    if ( (sz > 2) ) {
      if ( (i > 1) ) {
        ret.add(cmd);
      }
    }
    ret.add(tx_STAR(ast[i], env));
  }
  return wrap(ret, "(", ")");
};
SPEC_DASH_OPS["bitwise-lshift"] = sf_DASH_arithOp;
SPEC_DASH_OPS["bitwise-rshift"] = sf_DASH_arithOp;
SPEC_DASH_OPS["bitwise-zrshift"] = sf_DASH_arithOp;
SPEC_DASH_OPS["+"] = sf_DASH_arithOp;
SPEC_DASH_OPS["-"] = sf_DASH_arithOp;
SPEC_DASH_OPS["*"] = sf_DASH_arithOp;
SPEC_DASH_OPS["/"] = sf_DASH_arithOp;
SPEC_DASH_OPS["div"] = sf_DASH_arithOp;
SPEC_DASH_OPS["mod"] = sf_DASH_arithOp;
SPEC_DASH_OPS["or"] = sf_DASH_arithOp;
SPEC_DASH_OPS["and"] = sf_DASH_arithOp;
SPEC_DASH_OPS["exp"] = sf_DASH_arithOp;
SPEC_DASH_OPS["rem"] = sf_DASH_arithOp;
SPEC_DASH_OPS["bitwise-and"] = sf_DASH_arithOp;
SPEC_DASH_OPS["bitwise-or"] = sf_DASH_arithOp;
SPEC_DASH_OPS["bitwise-not"] = sf_DASH_arithOp;
SPEC_DASH_OPS["bitwise-xor"] = sf_DASH_arithOp;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-do] in file: compiler.ky near line: 655
const sf_DASH_do = function(ast, env) {
  assertArity(true, ast);
  let ret = node_QTE(ast);
  let stmtQ = stmt_QUERY(ast);
  ret.add(txDo(exprHint(xfi(ast, ast.slice(1)), (!stmtQ)), env, (!stmtQ)));
  return (stmtQ ?
    wrap(ret, null, "") :
    wrap(ret, "(function() {\n", "}).call(this)"));
};
SPEC_DASH_OPS["do"] = sf_DASH_do;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-case] in file: compiler.ky near line: 668
const sf_DASH_case = function(ast, env) {
  assertArity((kirbystdlibref.count(ast) >= 4), ast);
  let stmtQ = stmt_QUERY(ast);
  let ret = node_QTE(ast);
  let tst = ast[1];
  let brk = ";\nbreak;\n";
  let gs = [gensym("C__")].join("");
  let e,
    t,
    c,
    dft;
  if ( (!(0 === kirbystdlibref.modulo(kirbystdlibref.count(ast), 2))) ) {
    dft = pop_BANG(ast)[0];
  }
  for (let i = 2, sz = kirbystdlibref.count(ast), ____break = false; ((!____break) && (i < sz)); i = (i + 2)) {
    (e = ast[i], c = ast[i + 1]);
    if (pairs_QUERY(e)) {
      for (let j = 0, sz = kirbystdlibref.count(e), ____break = false; ((!____break) && (j < sz)); j = (j + 1)) {
        ret.add(["case ", tx_STAR(e[j], env), ":\n"]);
        if ( (j === (e.length - 1)) ) {
          ret.add([gs, "=", tx_STAR(c, env), brk]);
        }
      }
    } else {
      ret.add(["case ", tx_STAR(e, env), ":\n", gs, "=", tx_STAR(c, env), brk]);
    }
  }
  if (dft) {
    ret.add(["default:\n", gs, "=", tx_STAR(dft, env), brk]);
  }
  wrap(ret, ["switch (", tx_STAR(tst, env), ") {\n"], "}");
  return (stmtQ ?
    wrap(ret, ["let ", gs, ";\n"], "") :
    wrap(ret, ["(function() { let ", gs, ";\n"], ["return ", gs, ";}).call(this)"]));
};
SPEC_DASH_OPS["case"] = sf_DASH_case;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-vardefs] in file: compiler.ky near line: 702
const sf_DASH_vardefs = function(ast, env) {
  assertArity((kirbystdlibref.count(ast) >= 2), ast);
  let ret = node_QTE(ast);
  let cmd = [ast[0]].join("");
  let keys = {};
  let private_QUERY = (cmd !== "def~");
  if (cmd.startsWith("def")) {
    cmd = "var";
  } else {
    cmd = "let";
  }
  for (let i = 1, s = null, sz = kirbystdlibref.count(ast), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    s = tx_STAR(ast[i], env);
    keys[s] = null;
    if ( (i > 1) ) {
      ret.add(",");
    }
    ret.add(s);
  }
  wrap(ret, [cmd, " "].join(""), ";\n");
  if ( ((!private_QUERY) && (1 === eng.globalEnv().countNSPCache())) ) {
    let G__7 = keys;
    Object.entries(G__7).forEach(function(e) {
      return (function(v, k) {
        return _STAR_externs_STAR[k] = k;
      })(e[1], e[0]);
    });
  }
  return ret;
};
SPEC_DASH_OPS["def~-"] = sf_DASH_vardefs;
SPEC_DASH_OPS["def~"] = sf_DASH_vardefs;
SPEC_DASH_OPS["var~"] = sf_DASH_vardefs;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-var] in file: compiler.ky near line: 725
const sf_DASH_var = function(ast, env) {
  assertArity((!(0 === kirbystdlibref.modulo(kirbystdlibref.count(ast), 2))), ast);
  let ret = node_QTE(ast);
  let cmd = [ast[0]].join("");
  let keys = [];
  let tmp = null;
  let vname = null;
  let private_QUERY = ((cmd !== "const") && (cmd !== "def"));
  let rval,
    rc,
    lhs,
    rhs;
  cmd = (cmd.startsWith("const") ?
    "const" :
    ((cmd == "var") ?
      "let" :
      "var"));
  for (let i = 1, sz = kirbystdlibref.count(ast), ____break = false; ((!____break) && (i < sz)); i = (i + 2)) {
    (lhs = ast[i], rhs = ast[i + 1], rval = tx_STAR(rhs, env));
    if (symbol_QUERY(lhs)) {
      (lhs = tx_STAR(lhs, env), tmp = [cmd, " ", lhs, "=", rval, ";\n"]);
      conj_BANG(keys, lhs);
      ret.add(tmp);
    } else {
      (out = node_QTE(ast), rhs = destruct_STAR(lhs, out, env, ast));
      ret.add(["let ", tx_STAR(rhs, env), "=", rval, ";\n", [out].join("")]);
    }
  }
  if ( ((!private_QUERY) && (1 === eng.globalEnv().countNSPCache())) ) {
    keys.forEach(function(k) {
      return _STAR_externs_STAR[k] = k;
    });
  }
  return ret;
};
SPEC_DASH_OPS["const-"] = sf_DASH_var;
SPEC_DASH_OPS["const"] = sf_DASH_var;
SPEC_DASH_OPS["def-"] = sf_DASH_var;
SPEC_DASH_OPS["def"] = sf_DASH_var;
SPEC_DASH_OPS["var"] = sf_DASH_var;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-inst?] in file: compiler.ky near line: 763
const sf_DASH_inst_QUERY = function(ast, env) {
  assertArity((kirbystdlibref.count(ast) === 3), ast);
  let ret = node_QTE(ast);
  return wrap(ret, null, ["(", tx_STAR(ast[2], env), " instanceof ", tx_STAR(ast[1], env), ")"]);
};
SPEC_DASH_OPS["inst?"] = sf_DASH_inst_QUERY;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-delete] in file: compiler.ky near line: 771
const sf_DASH_delete = function(ast, env) {
  assertArity(((kirbystdlibref.count(ast) >= 2) && (kirbystdlibref.count(ast) < 4)), ast);
  let ret = node_QTE(ast);
  ret.add(["delete ", tx_STAR(ast[1], env)]);
  if ( (kirbystdlibref.count(ast) > 2) ) {
    ret.add(["[", tx_STAR(ast[2], env), "]"]);
  }
  return ret;
};
SPEC_DASH_OPS["delete!"] = sf_DASH_delete;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-new] in file: compiler.ky near line: 782
const sf_DASH_new = function(ast, env) {
  assertArity((kirbystdlibref.count(ast) >= 2), ast);
  let ret = node_QTE(ast);
  return wrap(ret, "new ", tx_STAR(xfi(ast, ast.slice(1)), env));
};
SPEC_DASH_OPS["new"] = sf_DASH_new;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-throw] in file: compiler.ky near line: 789
const sf_DASH_throw = function(ast, env) {
  assertArity((kirbystdlibref.count(ast) >= 2), ast);
  let ret = node_QTE(ast);
  let stmtQ = stmt_QUERY(ast);
  ret.add(["throw ", tx_STAR(xfi(ast, ast[1]), env)]);
  if ( (!stmtQ) ) {
    wrap(ret, "(function (){ ", ";}).call(this)");
  }
  return ret;
};
SPEC_DASH_OPS["throw"] = sf_DASH_throw;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-x-opop] in file: compiler.ky near line: 801
const sf_DASH_x_DASH_opop = function(ast, env) {
  assertArity((kirbystdlibref.count(ast) === 2), ast);
  let ret = node_QTE(ast);
  let cmd = [ast[0]].join("");
  let a2 = tx_STAR(ast[1], env);
  if (cmd.endsWith("$")) {
    ret.add([a2, cmd.slice(0, -1)]);
  } else {
    ret.add([cmd, a2]);
  }
  return ret;
};
SPEC_DASH_OPS["++"] = sf_DASH_x_DASH_opop;
SPEC_DASH_OPS["--"] = sf_DASH_x_DASH_opop;
SPEC_DASH_OPS["++$"] = sf_DASH_x_DASH_opop;
SPEC_DASH_OPS["--$"] = sf_DASH_x_DASH_opop;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-x-eq] in file: compiler.ky near line: 812
const sf_DASH_x_DASH_eq = function(ast, env) {
  assertArity((kirbystdlibref.count(ast) === 3), ast);
  let cmd = [ast[0]].join("");
  let ret = node_QTE(ast);
  if ( (cmd == "bitwise-zrshift=") ) {
    cmd = ">>>=";
  } else {
    if ( (cmd == "bitwise-rshift=") ) {
      cmd = ">>=";
    } else {
      if ( (cmd == "bitwise-lshift=") ) {
        cmd = "<<=";
      } else {
        if ( (cmd == "bitwise-xor=") ) {
          cmd = "^=";
        } else {
          if ( (cmd == "bitwise-or=") ) {
            cmd = "|=";
          } else {
            if ( (cmd == "bitwise-and=") ) {
              cmd = "&=";
            } else {
              if ( (cmd == "div=") ) {
                cmd = "/=";
              } else {
                if ( (cmd == "rem=") ) {
                  cmd = "%=";
                } else {
                  if ( (cmd == "exp=") ) {
                    cmd = "**=";
                  } else {
                    null;
                  }
                }
              }
            }
          }
        }
      }
    }
  }
  return wrap(ret, null, [tx_STAR(ast[1], env), " ", cmd, " ", tx_STAR(ast[2], env)]);
};
SPEC_DASH_OPS["+="] = sf_DASH_x_DASH_eq;
SPEC_DASH_OPS["-="] = sf_DASH_x_DASH_eq;
SPEC_DASH_OPS["*="] = sf_DASH_x_DASH_eq;
SPEC_DASH_OPS["div="] = sf_DASH_x_DASH_eq;
SPEC_DASH_OPS["rem="] = sf_DASH_x_DASH_eq;
SPEC_DASH_OPS["exp="] = sf_DASH_x_DASH_eq;
SPEC_DASH_OPS["bitwise-and="] = sf_DASH_x_DASH_eq;
SPEC_DASH_OPS["bitwise-or="] = sf_DASH_x_DASH_eq;
SPEC_DASH_OPS["bitwise-xor="] = sf_DASH_x_DASH_eq;
SPEC_DASH_OPS["bitwise-lshift="] = sf_DASH_x_DASH_eq;
SPEC_DASH_OPS["bitwise-rshift="] = sf_DASH_x_DASH_eq;
SPEC_DASH_OPS["bitwise-zrshift="] = sf_DASH_x_DASH_eq;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-set-in] in file: compiler.ky near line: 843
const sf_DASH_set_DASH_in = function(ast, env) {
  assertArity((0 === kirbystdlibref.modulo(kirbystdlibref.count(ast), 2)), ast);
  let ret = node_QTE(ast);
  let more = false;
  let obj = tx_STAR(ast[1], env);
  for (let i = 2, sz = kirbystdlibref.count(ast), ____break = false; ((!____break) && (i < sz)); i = (i + 2)) {
    if ( (i > 2) ) {
      ret.add(",");
      more = true;
    }
    ret.add([obj, "[", tx_STAR(xfi(ast, ast[i]), env), "]", "=", tx_STAR(xfi(ast, ast[i + 1]), env)]);
  }
  if (more) {
    wrap(ret, "(", ")");
  }
  return ret;
};
SPEC_DASH_OPS["set-in!"] = sf_DASH_set_DASH_in;
SPEC_DASH_OPS["=>>"] = sf_DASH_set_DASH_in;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-set] in file: compiler.ky near line: 860
const sf_DASH_set = function(ast, env) {
  assertArity((!(0 === kirbystdlibref.modulo(kirbystdlibref.count(ast), 2))), ast);
  let more = false;
  let ret = node_QTE(ast);
  for (let i = 1, sz = kirbystdlibref.count(ast), ____break = false; ((!____break) && (i < sz)); i = (i + 2)) {
    if ( (i > 1) ) {
      ret.add(",");
      more = true;
    }
    ret.add([tx_STAR(ast[i], env), "=", tx_STAR(xfi(ast, ast[i + 1]), env)]);
  }
  if (more) {
    wrap(ret, "(", ")");
  }
  return ret;
};
SPEC_DASH_OPS["set!"] = sf_DASH_set;
SPEC_DASH_OPS["=>"] = sf_DASH_set;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-fn] in file: compiler.ky near line: 875
//(fn [x y] ...)
const sf_DASH_fn = function(ast, env) {
  assertArity((kirbystdlibref.count(ast) >= 2), ast);
  let ret = node_QTE(ast);
  let fargs = null;
  let body = xfi(ast, Array.prototype.slice.call(ast, 2));
  let G____16 = meta_QUERY__QUERY(ast[1], env);
  let args = G____16[1];
  if ( (!Array.isArray(args)) ) {
    error_BANG("invalid-fargs", ast);
  }
  fargs = doFuncArgs(xfi(ast, args), ast, env);
  return wrap(ret, null, ["function (", fargs[0], ") {\n", fargs[1], txDo(body, env, true), "}"]);
};
SPEC_DASH_OPS["fn"] = sf_DASH_fn;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-func] in file: compiler.ky near line: 892
const sf_DASH_func = function(ast, env) {
  assertArity((kirbystdlibref.count(ast) >= 2), ast);
  let private_QUERY = [ast[0]].join("").endsWith("-");
  let mtd_QUERY = ([ast[0]].join("") == "method");
  let fname0 = [ast[1]].join("");
  let fname = [tx_STAR(ast[1], env)].join("");
  let dot_QUERY = contains_QUERY(fname, ".");
  let fargs = null;
  let ret = node_QTE(ast, tnodeEx(fname));
  let G____17 = ((typeof (ast[2]) === "string") ?
    [ast[2], 3] :
    [null, 2]);
  let doc = G____17[0];
  let pargs = G____17[1];
  let body = xfi(ast, Array.prototype.slice.call(ast, (pargs + 1)));
  let G____18 = meta_QUERY__QUERY(ast[pargs], env);
  let hints = G____18[0];
  let args = G____18[1];
  if ( (!Array.isArray(args)) ) {
    error_BANG("invalid-fargs", ast);
  }
  fargs = doFuncArgs(xfi(ast, args), ast, env);
  hints = (hints || {});
  if (mtd_QUERY) {
    if (hints.static) {
      ret.add("static ");
    }
    ret.add([fname, " ("]);
    if ( (fname == "constructor") ) {
      conj_BANG(body, kirbystdlibref.symbol("this"));
    }
  } else {
    if (dot_QUERY) {
      ret.add([fname, " = function ("]);
    } else {
      if (true) {
        ret.add(["const ", fname, " = function ("]);
      }
    }
  }
  ret.add([fargs[0], ") {\n", fargs[1], txDo(body, env, true), "};\n"]);
  if (not_DASH_empty(hints)) {
    ret.add(fmtSpecOps(fname, hints));
  }
  if (doc) {
    ret.prepend(writeDoc(doc));
  }
  ret.prepend(writeFuncInfo(fname0, ast));
  if ( ((!private_QUERY) && (!dot_QUERY) && (!mtd_QUERY) && (1 === eng.globalEnv().countNSPCache())) ) {
    _STAR_externs_STAR[fname] = fname;
  }
  return ret;
};
SPEC_DASH_OPS["defn"] = sf_DASH_func;
SPEC_DASH_OPS["defn-"] = sf_DASH_func;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-try] in file: compiler.ky near line: 937
const sf_DASH_try = function(ast, env) {
  assertArity((kirbystdlibref.count(ast) >= 2), ast);
  let stmtQ = stmt_QUERY(ast);
  let sz = kirbystdlibref.count(ast);
  let ret = node_QTE(ast);
  let t,
    f,
    c;
  f = last(ast);
  if ( (Array.isArray(f) && ("finally" == [f[0]].join(""))) ) {
    pop_BANG(ast);
    sz = kirbystdlibref.count(ast);
    xfi(f[0], f);
  } else {
    f = null;
  }
  c = null;
  if ( (sz > 1) ) {
    c = ast[(sz - 1)];
  }
  if ( (Array.isArray(c) && ("catch" == [c[0]].join(""))) ) {
    if ( ((kirbystdlibref.count(c) < 2) || (!symbol_QUERY(c[1]))) ) {
      error_BANG("invalid-catch", ast);
    }
    pop_BANG(ast);
    xfi(c[0], c);
  } else {
    c = null;
  }
  if ( ((f === null) && (c === null)) ) {
    error_BANG("invalid-try", ast);
  }
  ret.add(["try {\n", txDo(exprHint(xfi(ast, ast.slice(1)), (!stmtQ)), env), "\n}"]);
  if (c) {
    t = c[1];
    ret.add([["catch (", tx_STAR(t, env), ") {\n"].join(""), txDo(exprHint(xfi(c, Array.prototype.slice.call(c, 2)), (!stmtQ)), env), ";\n}\n"]);
  }
  if (f) {
    ret.add(["finally {\n", txDo(exprHint(xfi(f, f.slice(1)), false), env, false), ";\n}\n"]);
  }
  if ( (!stmtQ) ) {
    wrap(ret, "(function(){\n", "}).call(this)");
  }
  return ret;
};
SPEC_DASH_OPS["try"] = sf_DASH_try;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-if] in file: compiler.ky near line: 981
const sf_DASH_if = function(ast, env) {
  assertArity((kirbystdlibref.count(ast) >= 3), ast);
  let stmtQ = stmt_QUERY(ast);
  let ret = node_QTE(ast);
  let a1 = exprHint(xfi(ast, ast[1]), true);
  let a2 = exprHint(xfi(ast, ast[2]), (!stmtQ));
  let m_QUERY = (kirbystdlibref.count(ast) > 3);
  let a3 = (m_QUERY ?
    xfi(ast, ast[3]) :
    null);
  let elze = (m_QUERY ?
    exprHint(a3, (!stmtQ)) :
    null);
  (a1 = tx_STAR(a1, env), a2 = tx_STAR(a2, env), elze = tx_STAR(elze, env));
  return wrap(ret, null, (stmtQ ?
    ["if (", a1, ") {\n", a2, ";\n}",(m_QUERY ?
      [" else { \n", elze, ";\n}"] :
      "")] :
    ["(", a1, " ?\n", a2, " :\n",(elze || "null"), ")"]));
};
SPEC_DASH_OPS["if"] = sf_DASH_if;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-get] in file: compiler.ky near line: 1001
const sf_DASH_get = function(ast, env) {
  assertArity((kirbystdlibref.count(ast) === 3), ast);
  let ret = node_QTE(ast);
  return wrap(ret, null, [tx_STAR(xfi(ast, ast[1]), env), "[", tx_STAR(xfi(ast, ast[2]), env), "]"]);
};
SPEC_DASH_OPS["nth"] = sf_DASH_get;
SPEC_DASH_OPS["get"] = sf_DASH_get;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-array] in file: compiler.ky near line: 1009
const sf_DASH_array = function(ast, env) {
  assertArity(true, ast);
  let pos = 0;
  let ret = node_QTE(ast);
  if ( (0 === kirbystdlibref.count(ast)) ) {
    ret.add("[]");
  } else {
    if ( (!vector_QUERY(ast)) ) {
      pos = 1;
    }
    ret.add("[\n");
    for (let i = pos, sz = kirbystdlibref.count(ast), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
      if ( (i > pos) ) {
        ret.add(",\n");
      }
      ret.add(tx_STAR(xfi(ast, ast[i]), env));
    }
    ret.add("\n]");
  }
  return ret;
};
SPEC_DASH_OPS["vec"] = sf_DASH_array;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-object] in file: compiler.ky near line: 1025
const sf_DASH_object = function(ast, env) {
  assertArity(true, ast);
  let pos = 0;
  let ret = node_QTE(ast);
  if ( (0 === kirbystdlibref.count(ast)) ) {
    ret.add("{}");
  } else {
    if ( (!map_QUERY(ast)) ) {
      pos = 1;
    }
    ret.add("{\n");
    for (let i = pos, sz = kirbystdlibref.count(ast), ____break = false; ((!____break) && (i < sz)); i = (i + 2)) {
      if ( (i > pos) ) {
        ret.add(",\n");
      }
      ret.add([tx_STAR(ast[i], env), ": ", tx_STAR(xfi(ast, ast[i + 1]), env)]);
    }
    ret.add("\n}");
  }
  return ret;
};
SPEC_DASH_OPS["hashmap"] = sf_DASH_object;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-include] in file: compiler.ky near line: 1042
const sf_DASH_include = function(ast, env) {
  assertArity((kirbystdlibref.count(ast) >= 2), ast);
  let ret = [];
  let dir = path.dirname(ast.source);
  let fname,
    e;
  for (let i = 1, sz = kirbystdlibref.count(ast), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    e = ast[i];
    if ( ((!Array.isArray(e)) || (1 !== kirbystdlibref.count(e))) ) {
      error_BANG("invalid-include", ast);
    }
    fname = [e[0]].join("");
    if (fname) {
      fname = fname.replace(/[\"]/g, "");
    }
    try {
      fname = fs.realpathSync([dir, "/", fname].join(""));
    } catch (e) {
      error_BANG("file-access", ast);
    }
    try {
      conj_BANG(ret, includeFile(fname, ast));
    } catch (e) {
      error_BANG("file-read", ast);
    }
    finally {
      eng.globalEnv().popNSP();
    }
  }
  return ((0 === kirbystdlibref.count(ret)) ?
    "" :
    ret);
};
////////////////////////////////////////////////////////////////////////////////
//name: [sf-require] in file: compiler.ky near line: 1070
const sf_DASH_require = function(ast, env) {
  assertArity((kirbystdlibref.count(ast) >= 2), ast);
  let as,
    path,
    v,
    e,
    refers,
    renames;
  let ret = node_QTE(ast);
  for (let i = 1, sz = kirbystdlibref.count(ast), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    (e = ast[i], as = [gensym("R__")].join(""), refers = null, renames = null);
    if ( ((!Array.isArray(e)) || (kirbystdlibref.count(e) < 3)) ) {
      error_BANG("invalid-require", ast);
    }
    path = [e[0]].join("");
    for (let j = 1, sz = kirbystdlibref.count(e), ____break = false; ((!____break) && (j < sz)); j = (j + 1)) {
      v = e[j];
      if ( (v == "as") ) {
        as = [e[j + 1]].join("");
        ++j;
      } else {
        if ( (v == "refer") ) {
          refers = e[j + 1];
          ++j;
        } else {
          if ( (v == "rename") ) {
            renames = e[j + 1];
            ++j;
          } else {
            null;
          }
        }
      }
    }
    ret.add(["const ", parser.jsid(as), "= require(", tx_STAR(path, env), ");\n"]);
    for (let i = 0, sz = kirbystdlibref.count(refers), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
      v = tx_STAR(refers[i], env);
      ret.add(["const ", v, "=", as, "[\"", v, "\"];\n"]);
    }
    for (let i = 0, sz = kirbystdlibref.count(renames), ____break = false; ((!____break) && (i < sz)); i = (i + 2)) {
      (e = tx_STAR(renames[i], env), v = tx_STAR(renames[i + 1], env));
      ret.add(["const ", v, "=", as, "[\"", e, "\"];\n"]);
    }
  }
  return ret;
};
////////////////////////////////////////////////////////////////////////////////
//name: [sf-ns] in file: compiler.ky near line: 1108
const sf_DASH_ns = function(ast, env) {
  assertArity((kirbystdlibref.count(ast) >= 2), ast);
  let ret = [];
  let e = null;
  let G____19 = meta_QUERY__QUERY(ast[1], env);
  let hints = G____19[0];
  let nsp = G____19[1];
  if ( (!symbol_QUERY(nsp)) ) {
    error_BANG("invalid-namespace", ast);
  }
  eng.globalEnv().pushNSP([nsp].join(""));
  ast = xfi(ast, Array.prototype.slice.call(ast, 2));
  for (let i = 0, sz = kirbystdlibref.count(ast), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    e = ast[i];
    if ( (pairs_QUERY(e) && ("include" == [e[0]].join(""))) ) {
      conj_BANG(ret, sf_DASH_include(xfi(ast, e)));
    } else {
      if ( (pairs_QUERY(e) && ("require" == [e[0]].join(""))) ) {
        conj_BANG(ret, sf_DASH_require(xfi(ast, e)));
      } else {
        null;
      }
    }
  }
  nsp = eng.globalEnv().peekNSP();
  if ( ((nsp === "czlab.kirby.macros") || (nsp === "czlab.kirby.stdlib")) ) {
    null;
  } else {
    if (nsp.startsWith("czlab.kirby.")) {
      conj_BANG(ret, "const kirbystdlibref=std;\n");
    } else {
      if (true) {
        conj_BANG(ret, sf_DASH_require(xfi(ast, [kirbystdlibref.symbol("require"), [kirby, kirbystdlibref.keyword(":as"), kirbystdlibref.symbol("kirbystdlibref")]])));
      }
    }
  }
  return ret;
};
SPEC_DASH_OPS["ns"] = sf_DASH_ns;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-comment] in file: compiler.ky near line: 1139
const sf_DASH_comment = function(ast, env) {
  return "";
};
SPEC_DASH_OPS["comment"] = sf_DASH_comment;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-for] in file: compiler.ky near line: 1142
const sf_DASH_for = function(ast, env) {
  assertArity((kirbystdlibref.count(ast) >= 2), ast);
  let stmtQ = stmt_QUERY(ast);
  let ret = node_QTE(ast);
  let body = exprHint(xfi(ast, Array.prototype.slice.call(ast, 2)), false);
  if ( (0 === kirbystdlibref.count(body)) ) {
    node_QTE(ast);
  } else {
    sf_DASH_floop(ret, xfi(ast, ast[1]), body, env, stmtQ);
  }
  return ret;
};
SPEC_DASH_OPS["for"] = sf_DASH_for;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-floop] in file: compiler.ky near line: 1154
const sf_DASH_floop = function(ret, args, body, env, stmtQ) {
  let vars = [];
  let recurs = [];
  let tst,
    nb,
    e;
  for (let i = 0, sz = kirbystdlibref.count(args), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    e = args[i];
    if ( (e == "while") ) {
      tst = args[i + 1];
      ++i;
    } else {
      if ( (e == "recur") ) {
        recurs = Array.prototype.slice.call(args, (i + 1));
        ____break = true;
      } else {
        if (symbol_QUERY(e)) {
          conj_BANG(vars, e, args[i + 1]);
          ++i;
        } else {
          null;
        }
      }
    }
  }
  ret.add("for (");
  for (let i = 0, sz = kirbystdlibref.count(vars), ____break = false; ((!____break) && (i < sz)); i = (i + 2)) {
    if ( (i === 0) ) {
      ret.add("let ");
    }
    if ( (i !== 0) ) {
      ret.add(",");
    }
    ret.add([tx_STAR(vars[i], env), "=", [tx_STAR(vars[i + 1], env)].join("")]);
  }
  ret.add(((0 === kirbystdlibref.count(vars)) ?
    "let ____break=false;" :
    ",____break=false;"));
  nb = [kirbystdlibref.symbol("not"), kirbystdlibref.symbol("____break")];
  xfi(ret, nb);
  if (tst) {
    tst = [kirbystdlibref.symbol("and"), nb, tst];
  } else {
    tst = nb;
  }
  xfi(ret, tst);
  ret.add([[tx_STAR(tst, env)].join(""), "; "]);
  for (let i = 0, k = 0, sz = kirbystdlibref.count(recurs), ____break = false; ((!____break) && (i < sz)); i = (i + 1), k = (k + 2)) {
    if ( (i !== 0) ) {
      ret.add(",");
    }
    ret.add([tx_STAR(vars[k], env), "=", [tx_STAR(recurs[i], env)].join("")]);
  }
  ret.add(["){\n", txDo(body, env, false), "}\n"]);
  if ( (!stmtQ) ) {
    wrap(ret, "(function() {\n", "}).call(this)");
  }
  return ret;
};
////////////////////////////////////////////////////////////////////////////////
//name: [sf-jscode] in file: compiler.ky near line: 1194
const sf_DASH_jscode = function(ast, env) {
  assertArity((kirbystdlibref.count(ast) >= 2), ast);
  let s = [ast[1]].join("");
  let name = parser.jsid("sf-jscode");
  return node_QTE(ast, tnodeEx(name, ((s.endsWith("\"") && s.startsWith("\"")) ?
    s.slice(1, -1) :
    s)));
};
SPEC_DASH_OPS["js#"] = sf_DASH_jscode;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-macro] in file: compiler.ky near line: 1204
const sf_DASH_macro = function(ast, env) {
  assertArity((kirbystdlibref.count(ast) >= 2), ast);
  let pms = [];
  let args = ast[2];
  let body = Array.prototype.slice.call(ast, 3);
  let doc,
    x,
    e,
    ev;
  if ( (typeof (args) === "string") ) {
    (doc = args, args = ast[3], body = Array.prototype.slice.call(ast, 4));
  }
  for (let i = 0, sz = kirbystdlibref.count(args), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    (e = args[i], ev = [e].join(""));
    if ( (ev === "&") ) {
      if (Array.isArray(args[i + 1])) {
        e = args[i + 1];
        ++i;
        for (let j = 0, sz = kirbystdlibref.count(e), ____break = false; ((!____break) && (j < sz)); j = (j + 1)) {
          x = e[j];
          if ( (!symbol_QUERY(x)) ) {
            error_BANG("syntax-error", ast);
          }
          conj_BANG(pms, x);
        }
      } else {
        conj_BANG(pms, e, args[i + 1]);
        ++i;
      }
    } else {
      if ( (!symbol_QUERY(e)) ) {
        error_BANG("syntax-error", ast);
      } else {
        if (true) {
          conj_BANG(pms, e);
        }
      }
    }
  }
  let a2 = [kirbystdlibref.symbol("fn*"), pms].concat(body);
  let func = eng.compute(a2, env);
  let a1 = [ast[1]].join("");
  (func["____macro"] = true, func["____doc"] = doc);
  eng.setMacro(a1, func);
  return "";
};
SPEC_DASH_OPS["defmacro"] = sf_DASH_macro;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-unary] in file: compiler.ky near line: 1237
const sf_DASH_unary = function(ast, env) {
  assertArity((kirbystdlibref.count(ast) === 2), ast);
  let G____20 = ast;
  let a0 = G____20[0];
  let a1 = G____20[1];
  let ret = node_QTE(ast);
  if ( (a0 == "not") ) {
    a0 = kirbystdlibref.symbol("!");
  }
  ret.add(["(", [tx_STAR(a0, env)].join(""), [tx_STAR(a1, env)].join(""), ")"]);
  return ret;
};
SPEC_DASH_OPS["not"] = sf_DASH_unary;
SPEC_DASH_OPS["~"] = sf_DASH_unary;
SPEC_DASH_OPS["!"] = sf_DASH_unary;
////////////////////////////////////////////////////////////////////////////////
//name: [xfi] in file: compiler.ky near line: 1246
const xfi = function(from, to) {
  return ((from && to) ?
    (function() {
      let tline = to.line;
      let fline = from.line;
      if ( ((!(typeof (tline) === "number")) && (typeof (fline) === "number")) ) {
        (to["source"] = from.source, to["line"] = from.line, to["column"] = from.column);
      }
      return to;
    }).call(this) :
    (true ?
      to :
      null));
};
////////////////////////////////////////////////////////////////////////////////
//name: [spitExterns] in file: compiler.ky near line: 1260
const spitExterns = function() {
  let s = "";
  let ks = Object.keys(_STAR_externs_STAR);
  if (not_DASH_empty(ks)) {
    (s = ks.map(function() {
      let ____args = Array.prototype.slice.call(arguments);
      return [____args[0], ": ", ____args[0]].join("");
    }).join(",\n"), s = ["\n\nmodule.exports = {\n", s, "\n};\n\n"].join(""));
  }
  return s;
};
////////////////////////////////////////////////////////////////////////////////
//name: [banner] in file: compiler.ky near line: 1267
const banner = function() {
  return ["/*", "Auto generated by Kirby - v", MODULE_DASH_VERSION, " ", eng.globalEnv().firstNSP(), " - ", new Date(), "*/\n\n"].join("");
};
////////////////////////////////////////////////////////////////////////////////
//name: [cleanCode] in file: compiler.ky near line: 1273
const cleanCode = function(code) {
  let tmp = null;
  let arr = [];
  let G__8 = code.split("\n");
  for (let G__10 = 0, G__9 = false, ____break = false; ((!____break) && ((!G__9) && (G__10 < G__8.length))); G__10 = (G__10 + 1)) {
    let s = G__8[G__10];
    let tmp = s.trim();
    if ( (!true) ) {
      G__9 = true;
    }
    if ( ((!G__9) && true) ) {
      if ( (not_DASH_empty(tmp) && (tmp !== ";")) ) {
        conj_BANG(arr, s);
      }
    }
  }
  null;
  return arr.join("\n");
};
////////////////////////////////////////////////////////////////////////////////
//name: [transpile*] in file: compiler.ky near line: 1282
const transpile_STAR = function(source, fname, options) {
  eng.globalEnv().resetNSPCache();
  _STAR_externs_STAR = {};
  let ret = txTree(parser.parse(source, fname), eng.globalEnv());
  let G____21 = options;
  let source_DASH_map = G____21["source-map"];
  let format = G____21["format"];
  let fbase = path.basename(fname, ".ky");
  let extra = spitExterns();
  let fopts = {};
  let err = null;
  let cstr = (source_DASH_map ?
    (function() {
      let fmap = [fbase, ".js"].join("");
      let smap = [fbase, ".map"].join("");
      let sout = ret.toStringWithSourceMap({
        "skipValidation": true,
        "file": fmap
      });
      fs.writeFileSync(smap, sout.map);
      return [sout.code, extra, "\n//# sourceMappingURL=", path.relative(path.dirname(fname), smap)].join("");
    }).call(this) :
    [ret, extra].join(""));
  try {
    if (format) {
      cstr = esfmt.format(cstr, fopts);
    }
  } catch (e) {
    err = e;
  }
  cstr = cleanCode(cstr);
  return [((0 === kirbystdlibref.count(cstr)) ?
    "" :
    [banner(), cstr].join("")), err];
};
////////////////////////////////////////////////////////////////////////////////
//name: [transpile] in file: compiler.ky near line: 1312
const transpile = function(code, file) {
  let G____22 = Array.prototype.slice.call(arguments, 2);
  let options = G____22[0];
  return (function() {
    try {
      return transpile_STAR(code, file, opt_QUERY__QUERY(options, {}));
    } catch (e) {
      if (console) {
        console.log([e.stack].join(""));
      }
      return (function() {
        throw e ;
      }).call(this);
    }
  }).call(this);
};
////////////////////////////////////////////////////////////////////////////////
//name: [dbgAST] in file: compiler.ky near line: 1319
const dbgAST = function(source, fname) {
  return parser.dumpTree(parser.parse(source, fname), fname);
};
const version = MODULE_DASH_VERSION;
module.exports = {
  tnodeEx: tnodeEx,
  tnode: tnode,
  sf_DASH_juxt: sf_DASH_juxt,
  transpile: transpile,
  dbgAST: dbgAST,
  version: version
};