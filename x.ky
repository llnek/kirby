(ns poo.face)

(defn swap! "Change value inside the Atom,
            returning the new value"
  ^Any [a f & xs]
  (->> (concat [a.value] xs)
       (apply f this)
       (set! a.value )) (get a :value))













