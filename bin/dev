#!/bin/sh
COMPILER="bin/dev.js"
OUTDIR=out
#make and clean out dir
if [ ! -d $OUTDIR ]; then
  if [ -e $OUTDIR ]; then
    rm $OUTDIR
  fi
  mkdir $OUTDIR
else
  #rm $OUTDIR/*.js
  echo ""
fi

#compile files
for n in stdlib parser engine compiler main
do
  ${COMPILER} -f src/czlab/kirby/${n}.ky
done





