/*Auto generated by Kirby - v1.0.0 czlab.kirby.cg.transpiler Fri Nov 24 2017 00:24:17 GMT+1100 (AEDT)*/

const std = require("../bl/stdlib");
const lambda_DASH_arg_QUERY = std["lambda_DASH_arg_QUERY"];
const hashmap = std["hashmap"];
const pairs_QUERY = std["pairs_QUERY"];
const contains_QUERY = std["contains_QUERY"];
const last = std["last"];
const typeid = std["typeid"];
const prn = std["prn"];
const primitive = std["primitive"];
const map_QUERY = std["map_QUERY"];
const vector_QUERY = std["vector_QUERY"];
const opt_QUERY__QUERY = std["opt_QUERY__QUERY"];
const some_QUERY = std["some_QUERY"];
const primitive_QUERY = std["primitive_QUERY"];
const not_DASH_empty = std["not_DASH_empty"];
const conj_BANG = std["conj_BANG"];
const list_QUERY = std["list_QUERY"];
const extendAttr = std["extendAttr"];
const gensym = std["gensym"];
const count = std["count"];
const symbol = std["symbol"];
const keyword = std["keyword"];
const symbol_QUERY = std["symbol_QUERY"];
const keyword_QUERY = std["keyword_QUERY"];
const rdr = require("../bl/lexer");
const tnode = rdr["tnode"];
const tnodeEx = rdr["tnodeEx"];
const psr = require("../bl/parser");
const rt = require("../rt/runtime");
const fs = require("fs");
const path = require("path");
const esfmt = require("esformatter");
const kirbystdlibref = std;
const ERRORS_DASH_MAP = {
  "e0": "Syntax Error",
  "e1": "Empty statement",
  "e2": "Invalid characters in function name",
  "e3": "End of File encountered, unterminated string",
  "e4": "Closing square bracket, without an opening square bracket",
  "e5": "End of File encountered, unterminated array",
  "e6": "Closing curly brace, without an opening curly brace",
  "e7": "End of File encountered, unterminated javascript object '}'",
  "e8": "End of File encountered, unterminated parenthesis",
  "e9": "Invalid character in var name",
  "e10": "Extra chars at end of file. Maybe an extra ')'.",
  "e11": "Cannot Open include File",
  "e12": "Invalid no of arguments to ",
  "e13": "Invalid Argument type to ",
  "e14": "End of File encountered, unterminated regular expression",
  "e15": "Invalid vararg position, must be last argument.",
  "e16": "Invalid arity (args > expected) to ",
  "e17": "Invalid arity (args < expected) to "
};
const MODULE_DASH_VERSION = "1.0.0";
var EXTERNS = null;
var SPEC_DASH_OPS = {};
var MATH_DASH_OP_DASH_REGEX = /^[-+][0-9]+$/;
////////////////////////////////////////////////////////////////////////////////
//name: [simpleton?] in file: src/czlab/kirby/cg/transpiler.ky near line: 61
const simpleton_QUERY = function(ast) {
  return ((typeof (ast) === "undefined") || (typeof (ast) === "string") || (typeof (ast) === "number") || (typeof (ast) === "boolean") || (ast === null));
};
////////////////////////////////////////////////////////////////////////////////
//name: [exprHint] in file: src/czlab/kirby/cg/transpiler.ky near line: 68
const exprHint = function(ast, flag) {
  let x = ast;
  if (simpleton_QUERY(ast)) {
    x = primitive(ast);
  }
  x["____expr"] = flag;
  return x;
};
////////////////////////////////////////////////////////////////////////////////
//name: [stmt?] in file: src/czlab/kirby/cg/transpiler.ky near line: 76
const stmt_QUERY = function(ast) {
  if (simpleton_QUERY(ast)) {
    throw new Error("Cant test expr? on primitive");
  }
  return (false === ast.____expr);
};
////////////////////////////////////////////////////////////////////////////////
//name: [error!] in file: src/czlab/kirby/cg/transpiler.ky near line: 82
const error_BANG = function(e) {
  let G____8 = Array.prototype.slice.call(arguments, 1);
  let line = G____8[0];
  let file = G____8[1];
  let msg = G____8[2];
  return (function() {
    throw new Error([ERRORS_DASH_MAP[e],(msg ?
      [" : ", msg].join("") :
      null),(line ?
      ["\nLine no ", line].join("") :
      null),(file ?
      ["\nFile ", file].join("") :
      null)].join("")) ;
  }).call(this);
};
////////////////////////////////////////////////////////////////////////////////
//name: [syntax!] in file: src/czlab/kirby/cg/transpiler.ky near line: 89
const syntax_BANG = function(ecode) {
  let G____9 = Array.prototype.slice.call(arguments, 1);
  let ast = G____9[0];
  let cmd = G____9[1];
  return error_BANG(ecode, (ast ?
    ast.line :
    0), (ast ?
    ast.source :
    0), cmd);
};
////////////////////////////////////////////////////////////////////////////////
//name: [pad] in file: src/czlab/kirby/cg/transpiler.ky near line: 95
const pad = function(z) {
  return " ".repeat(z);
};
////////////////////////////////////////////////////////////////////////////////
//name: [testre?] in file: src/czlab/kirby/cg/transpiler.ky near line: 98
const testre_QUERY = function(re, x) {
  return (x ?
    re.test(x) :
    null);
};
////////////////////////////////////////////////////////////////////////////////
//name: [node'] in file: src/czlab/kirby/cg/transpiler.ky near line: 101
const node_QTE = function(obj, src) {
  if ( (obj && src && (!simpleton_QUERY(obj))) ) {
    (obj.source = src.source, obj.column = src.column, obj.line = src.line);
  }
  return obj;
};
////////////////////////////////////////////////////////////////////////////////
//name: [destructVec] in file: src/czlab/kirby/cg/transpiler.ky near line: 109
const destructVec = function(lhs, keys) {
  let as,
    e,
    ev;
  for (let i = 0, sz = kirbystdlibref.count(lhs), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    (e = lhs[i], ev = [e].join(""));
    if (symbol_QUERY(e)) {
      if ( (ev === "_") ) {
        null;
      } else {
        if ( (ev === "&") ) {
          keys[["&", lhs[i + 1]].join("")] = i;
          ++i;
        } else {
          if (ev.startsWith("&")) {
            keys[ev] = i;
          } else {
            if (true) {
              keys[ev] = i;
            }
          }
        }
      }
    } else {
      if (keyword_QUERY(e)) {
        if ( (ev === "as") ) {
          ++i;
          as = [lhs[i]].join("");
        } else {
          throw new Error(["bad keyword: :", ev].join(""));
        }
      } else {
        null;
      }
    }
  }
  return as;
};
////////////////////////////////////////////////////////////////////////////////
//name: [destructMap] in file: src/czlab/kirby/cg/transpiler.ky near line: 134
const destructMap = function(lhs, keys) {
  let as,
    e,
    ev;
  for (let i = 0, sz = kirbystdlibref.count(lhs), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    (e = lhs[i], ev = [e].join(""));
    if (keyword_QUERY(e)) {
      if ( ((ev === "keys") || (ev === "strs")) ) {
        let ks = lhs[i + 1];
        ++i;
        for (let j = 0, sz = kirbystdlibref.count(ks), ____break = false; ((!____break) && (j < sz)); j = (j + 1)) {
          keys[[ks[j]].join("")] = null;
        }
      } else {
        if ( (ev === "as") ) {
          ++i;
          as = [lhs[i]].join("");
        } else {
          null;
        }
      }
    } else {
      throw new Error(["bad destruct field: ", typeid(e)].join(""));
    }
  }
  return as;
};
////////////////////////////////////////////////////////////////////////////////
//name: [destruct1] in file: src/czlab/kirby/cg/transpiler.ky near line: 155
const destruct1 = function(lhs) {
  let as = null;
  let out = [];
  let keys = {};
  if (vector_QUERY(lhs)) {
    as = destructVec(lhs, keys);
  } else {
    if (map_QUERY(lhs)) {
      as = destructMap(lhs, keys);
    } else {
      if (symbol_QUERY(lhs)) {
        keys[[lhs].join("")] = null;
      } else {
        if (true) {
          throw new Error(["cant destruct with: ", typeid(lhs)].join(""));
        }
      }
    }
  }
  return [as, keys];
};
////////////////////////////////////////////////////////////////////////////////
//name: [destruct0] in file: src/czlab/kirby/cg/transpiler.ky near line: 168
const destruct0 = function(cmd, lhs, rhs, env) {
  let G____10 = destruct1(lhs);
  let as = G____10[0];
  let keys = G____10[1];
  let kdefs = [];
  let ka = null;
  let kvals = tnode();
  if ( (0 === kirbystdlibref.count(as)) ) {
    as = gensym("A____");
  }
  as = rdr.jsid(as);
  let G__1 = keys;
  Object.entries(G__1).forEach(function(e) {
    return (function(v, k) {
      let rest_QUERY = false;
      if (k.startsWith("&")) {
        (rest_QUERY = true, k = k.slice(1));
      }
      k = rdr.jsid(k);
      conj_BANG(kdefs, k);
      return kvals.add([k, "=", as,((v === null) ?
        ["[\"", k, "\"];\n"].join("") :
        (rest_QUERY ?
          [".slice(", v, ");\n"].join("") :
          (true ?
            ["[", v, "];\n"].join("") :
            null)))].join(""));
    })(e[1], e[0]);
  });
  kvals.prepend([cmd, " ", kdefs.join(","), ";\n"].join(""));
  return [[tnodeEx([cmd, " ", rdr.jsid(as), "= ", tx_STAR(rhs, env), ";\n"]), kvals], kdefs];
};
////////////////////////////////////////////////////////////////////////////////
//name: [txTree] in file: src/czlab/kirby/cg/transpiler.ky near line: 198
const txTree = function(root, env) {
  let ret = node_QTE(tnode(), root);
  let G__2 = root;
  for (let G__4 = 0, G__3 = false, ____break = false; ((!____break) && ((!G__3) && (G__4 < G__2.length))); G__4 = (G__4 + 1)) {
    let r = G__2[G__4];
    let tmp = tx_STAR(r, env);
    if ( (!true) ) {
      G__3 = true;
    }
    if ( ((!G__3) && some_QUERY(tmp)) ) {
      ret.add([tmp, "\n"]);
    }
  }
  null;
  return ret;
};
////////////////////////////////////////////////////////////////////////////////
//name: [txExpr] in file: src/czlab/kirby/cg/transpiler.ky near line: 206
const txExpr = function(atoms, env) {
  return atoms.forEach(function(a, i, arr) {
    return arr[i] = tx_STAR(a, env);
  });
};
////////////////////////////////////////////////////////////////////////////////
//name: [txAtom] in file: src/czlab/kirby/cg/transpiler.ky near line: 211
const txAtom = function(a) {
  let s = [a].join("");
  return (lambda_DASH_arg_QUERY(a) ?
    ["____args[", s, "]"].join("") :
    (keyword_QUERY(a) ?
      ["\"", s, "\""].join("") :
      (symbol_QUERY(a) ?
        rdr.jsid(s) :
        ((a === null) ?
          "null" :
          (primitive_QUERY(a) ?
            (function() {
              a = a.value;
              return ((typeof (a) === "string") ?
                a :
                ((a === null) ?
                  "null" :
                  (true ?
                    [a].join("") :
                    null)));
            }).call(this) :
            (true ?
              s :
              null))))));
};
////////////////////////////////////////////////////////////////////////////////
//name: [tx*] in file: src/czlab/kirby/cg/transpiler.ky near line: 229
const tx_STAR = function(x) {
  let G____11 = Array.prototype.slice.call(arguments, 1);
  let env = G____11[0];
  return (function() {
    try {
      return (Array.isArray(x) ?
        txPairs(x, env) :
        txAtom(x));
    } catch (e) {
      if (console) {
        console.log(["Error in file: ", x.source, ", near line: ", x.line, ", column: ", x.column].join(""));
      }
      return (function() {
        throw e ;
      }).call(this);
    }
  }).call(this);
};
////////////////////////////////////////////////////////////////////////////////
//name: [findCmd] in file: src/czlab/kirby/cg/transpiler.ky near line: 239
const findCmd = function(ast) {
  return (vector_QUERY(ast) ?
    "vec" :
    (map_QUERY(ast) ?
      "hashmap" :
      (list_QUERY(ast) ?
        "list" :
        (Array.isArray(ast) ?
          [ast[0]].join("") :
          (true ?
            "" :
            null)))));
};
////////////////////////////////////////////////////////////////////////////////
//name: [quoteSingle] in file: src/czlab/kirby/cg/transpiler.ky near line: 251
const quoteSingle = function(a) {
  return (keyword_QUERY(a) ?
    ["keyword(\":", a, "\")"].join("") :
    (symbol_QUERY(a) ?
      ["symbol(\"", a, "\")"].join("") :
      (primitive_QUERY(a) ?
        (function() {
          a = a.value;
          return ((typeof (a) === "string") ?
            a :
            ((a === null) ?
              "null" :
              (true ?
                [a].join("") :
                null)));
        }).call(this) :
        (true ?
          [a].join("") :
          null))));
};
////////////////////////////////////////////////////////////////////////////////
//name: [quote!] in file: src/czlab/kirby/cg/transpiler.ky near line: 264
const quote_BANG = function(a, env) {
  return (Array.isArray(a) ?
    (map_QUERY(a) ?
      quoteMap(a, env) :
      quoteBlock(a, env)) :
    quoteSingle(a));
};
////////////////////////////////////////////////////////////////////////////////
//name: [quoteMap] in file: src/czlab/kirby/cg/transpiler.ky near line: 272
const quoteMap = function(a, env) {
  let ret = tnode();
  let comma = "";
  for (let i = 0, sz = kirbystdlibref.count(a), ____break = false; ((!____break) && (i < sz)); i = (i + 2)) {
    if ( (i > 0) ) {
      ret.add(",");
    }
    ret.add([quote_BANG(a[i], env), " : ", quote_BANG(a[i + 1], env)]);
  }
  if ( (!(0 === kirbystdlibref.count(a))) ) {
    comma = ",";
  }
  ret.prepend(["[", "symbol(\"hashmap\")", comma]);
  ret.add("]");
  return ret;
};
////////////////////////////////////////////////////////////////////////////////
//name: [quoteBlock] in file: src/czlab/kirby/cg/transpiler.ky near line: 285
const quoteBlock = function(a, env) {
  let ret = tnode();
  for (let i = 0, sz = kirbystdlibref.count(a), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    if ( (i > 0) ) {
      ret.add(",");
    }
    ret.add(quote_BANG(a[i], env));
  }
  ret.prepend("[");
  ret.add("]");
  return ret;
};
////////////////////////////////////////////////////////////////////////////////
//name: [sf-quote] in file: src/czlab/kirby/cg/transpiler.ky near line: 296
const sf_DASH_quote = function(ast, env) {
  let ret = node_QTE(tnode(), ast);
  ret.add(quote_BANG(ast[1], env));
  return ret;
};
SPEC_DASH_OPS["quote"] = sf_DASH_quote;
////////////////////////////////////////////////////////////////////////////////
//name: [txPairs] in file: src/czlab/kirby/cg/transpiler.ky near line: 302
const txPairs = function(ast, env) {
  let stmtQ = stmt_QUERY(ast);
  let cmd = findCmd(ast);
  let ret = tnode();
  let specop = null;
  let tmp = null;
  let mc = rt.getMacro(cmd);
  if (mc) {
    (ast = rt.expandMacro(ast, env, mc), ast = exprHint(ast, (!stmtQ)), cmd = findCmd(ast));
  }
  if (rdr.REGEX.int.test(cmd)) {
    let c0 = cmd.charAt(0);
    if ( ((c0 !== "-") && (c0 !== "+")) ) {
      cmd = ["+", cmd].join("");
    }
    (ast = [symbol(cmd.charAt(0)), ast[1], parseInt(cmd.slice(1))], cmd = [ast[0]].join(""));
  }
  specop = SPEC_DASH_OPS[cmd];
  if ( (cmd == "with-meta") ) {
    tmp = ast[1];
    if ( (!simpleton_QUERY(tmp)) ) {
      tmp["____meta"] = evalMeta(ast[2], env);
      ret.add(tx_STAR(tmp, env));
    } else {
      throw new Error("cant with-meta simple value");
    }
  } else {
    if (cmd.startsWith(".-")) {
      ret.add([tx_STAR(ast[1], env), ".", tx_STAR(symbol(cmd.slice(2)))]);
    } else {
      if (cmd.startsWith(".@")) {
        let onemore_QUERY = false;
        let pos = 2;
        if (cmd.startsWith(".@+")) {
          ++pos;
          onemore_QUERY = true;
        }
        ret.add([tx_STAR(ast[1], env), "[", cmd.slice(pos)]);
        if (onemore_QUERY) {
          ret.add("+1]");
        } else {
          ret.add("]");
        }
      } else {
        if ( ("." === cmd.charAt(0)) ) {
          ret.add([tx_STAR(ast[1], env), cmd, "("]);
          for (let n = 2, sz = kirbystdlibref.count(ast), ____break = false; ((!____break) && (n < sz)); n = (n + 1)) {
            if ( (n !== 2) ) {
              ret.add(",");
            }
            ret.add(tx_STAR(ast[n], env));
          }
          ret.add(")");
        } else {
          if (some_QUERY(specop)) {
            ret = specop(ast, env);
          } else {
            if (true) {
              if (pairs_QUERY(ast)) {
                txExpr(ast, env);
                cmd = ast[0];
              } else {
                cmd = tx_STAR(ast);
              }
              if ( (!cmd) ) {
                syntax_BANG("e1", ast);
              }
              cmd = maybeStripStdlib(cmd);
              if (pairs_QUERY(ast)) {
                if (testre_QUERY(rdr.REGEX.func, cmd)) {
                  cmd = tnodeEx(["(", cmd, ")"]);
                }
                ret.add([cmd, "(", tnodeEx(ast.slice(1)).join(","), ")"]);
              } else {
                ret.add(cmd);
              }
            }
          }
        }
      }
    }
  }
  return node_QTE(ret, ast);
};
////////////////////////////////////////////////////////////////////////////////
//name: [writeDoc] in file: src/czlab/kirby/cg/transpiler.ky near line: 375
//Convert to jsdoc
const writeDoc = function(doc) {
  let out = [];
  if (doc) {
    doc.replace(rdr.REGEX.dquoteHat, "").replace(rdr.REGEX.dquoteEnd, "").split("\\n").forEach(function() {
      let ____args = Array.prototype.slice.call(arguments);
      return (function() {
        let s = [____args[0]].join("").trim();
        return ((!(0 === kirbystdlibref.count(s))) ?
          conj_BANG(out, ["//", s, "\n"].join("")) :
          null);
      }).call(this);
    });
  }
  return out;
};
////////////////////////////////////////////////////////////////////////////////
//name: [sf-deftype] in file: src/czlab/kirby/cg/transpiler.ky near line: 388
//Define a JS Class
const sf_DASH_deftype = function(ast, env) {
  let private_QUERY = [ast[0]].join("").endsWith("-");
  let ret = node_QTE(tnode(), ast);
  let cz = tx_STAR(ast[1], env);
  let par = ast[2][0];
  let args = ast[3];
  let n,
    m,
    doc,
    mtds;
  if ( (typeof (ast[4]) === "string") ) {
    (doc = ast[4], mtds = ast.slice(5));
  } else {
    mtds = ast.slice(4);
  }
  ret.add(["class ", cz]);
  if (par) {
    ret.add([" extends ", tx_STAR(par, env)]);
  }
  ret.add(" {\n");
  for (let i = 0, pos = 0, sz = kirbystdlibref.count(mtds), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    m = mtds[i];
    pos = ((typeof (m[1]) === "string") ?
      3 :
      2);
    if ( ((m[0] == "constructor") && (!(0 === kirbystdlibref.count(args)))) ) {
      txExpr(args, env);
      for (let x = 0, sz = kirbystdlibref.count(args), ____break = false; ((!____break) && (x < sz)); x = (x + 2)) {
        args[x] = ["\"", args[x], "\""].join("");
      }
      m.splice(pos, 0, [symbol("set-in!"), symbol("this")].concat(args));
    }
    m.unshift(symbol("method"));
    ret.add(sf_DASH_func(m, env, false));
    ret.add("\n");
  }
  ret.add("}\n");
  if (doc) {
    ret.prepend(writeDoc(doc));
  }
  if ( ((!private_QUERY) && (1 === rt.globalEnv().countNSPCache())) ) {
    EXTERNS[cz] = cz;
  }
  return ret;
};
SPEC_DASH_OPS["deftype"] = sf_DASH_deftype;
SPEC_DASH_OPS["deftype-"] = sf_DASH_deftype;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-compOp] in file: src/czlab/kirby/cg/transpiler.ky near line: 432
const sf_DASH_compOp = function(ast, env) {
  let ret = node_QTE(tnode(), ast);
  let cmd = ast[0];
  if ( ((cmd == "not=") || (cmd == "!=")) ) {
    ast[0] = symbol("!==");
  }
  if ( (cmd == "=") ) {
    ast[0] = symbol("===");
  }
  txExpr(ast, env);
  for (let i = 0, op = ast.shift(), ____break = false; ((!____break) && (i < (ast.length - 1))); i = (i + 1)) {
    ret.add(tnodeEx([ast[i], " ", op, " ", ast[i + 1]]));
  }
  ret.join(" && ");
  ret.prepend("(");
  ret.add(")");
  return ret;
};
SPEC_DASH_OPS["not="] = sf_DASH_compOp;
SPEC_DASH_OPS["!="] = sf_DASH_compOp;
SPEC_DASH_OPS["=="] = sf_DASH_compOp;
SPEC_DASH_OPS["="] = sf_DASH_compOp;
SPEC_DASH_OPS[">"] = sf_DASH_compOp;
SPEC_DASH_OPS[">="] = sf_DASH_compOp;
SPEC_DASH_OPS["<"] = sf_DASH_compOp;
SPEC_DASH_OPS["<="] = sf_DASH_compOp;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-arithOp] in file: src/czlab/kirby/cg/transpiler.ky near line: 455
const sf_DASH_arithOp = function(ast, env) {
  let op = tnode();
  let ret = node_QTE(tnode(), ast);
  txExpr(ast, env);
  let e1 = ast.shift();
  let cmd = [e1].join("");
  if ( (cmd == "mod") ) {
    cmd = "%";
  }
  if ( (cmd == "div") ) {
    cmd = "/";
  }
  if ( (cmd == "and") ) {
    cmd = "&&";
  }
  if ( (cmd == "or") ) {
    cmd = "||";
  }
  if ( (1 === kirbystdlibref.count(ast)) ) {
    if ( ("-" == cmd) ) {
      ret.add("-");
    }
  } else {
    op.add(["", cmd, ""]);
  }
  ret.add(ast);
  if ( (kirbystdlibref.count(ast) > 1) ) {
    ret.join(op);
  }
  ret.prepend("(");
  ret.add(")");
  return ret;
};
SPEC_DASH_OPS["+"] = sf_DASH_arithOp;
SPEC_DASH_OPS["-"] = sf_DASH_arithOp;
SPEC_DASH_OPS["*"] = sf_DASH_arithOp;
SPEC_DASH_OPS["div"] = sf_DASH_arithOp;
SPEC_DASH_OPS["%"] = sf_DASH_arithOp;
SPEC_DASH_OPS["mod"] = sf_DASH_arithOp;
SPEC_DASH_OPS["or"] = sf_DASH_arithOp;
SPEC_DASH_OPS["and"] = sf_DASH_arithOp;
SPEC_DASH_OPS["^"] = sf_DASH_arithOp;
SPEC_DASH_OPS["&"] = sf_DASH_arithOp;
SPEC_DASH_OPS["|"] = sf_DASH_arithOp;
SPEC_DASH_OPS["<<"] = sf_DASH_arithOp;
SPEC_DASH_OPS[">>"] = sf_DASH_arithOp;
SPEC_DASH_OPS[">>>"] = sf_DASH_arithOp;
////////////////////////////////////////////////////////////////////////////////
//name: [txDo] in file: src/czlab/kirby/cg/transpiler.ky near line: 479
const txDo = function(ast, env) {
  let G____12 = Array.prototype.slice.call(arguments, 2);
  let return_QUERY = G____12[0];
  let ret = node_QTE(tnode(), ast);
  let stmtQ = stmt_QUERY(ast);
  let e = null;
  let end = (ast.length - 1);
  return_QUERY = (stmtQ ?
    false :
    opt_QUERY__QUERY(return_QUERY, true));
  for (let i = 0, ____break = false; ((!____break) && (i < end)); i = (i + 1)) {
    e = exprHint(ast[i], false);
    ret.add([tx_STAR(e, env), ";\n"]);
  }
  if ( (end >= 0) ) {
    (e = exprHint(ast[end], (!stmtQ)), e = tx_STAR(e, env));
    if ( (!return_QUERY) ) {
      ret.add([e, ";\n"]);
    } else {
      ret.add(["return ", e, ";\n"]);
    }
  }
  return ret;
};
////////////////////////////////////////////////////////////////////////////////
//name: [sf-do] in file: src/czlab/kirby/cg/transpiler.ky near line: 498
const sf_DASH_do = function(ast, env) {
  let ret = node_QTE(tnode(), ast);
  let stmtQ = stmt_QUERY(ast);
  let body = exprHint(ast.slice(1), (!stmtQ));
  ret.add(txDo(body, env, (!stmtQ)));
  if (stmtQ) {
    null;
  } else {
    ret.prepend("(function() {\n");
  }
  if (stmtQ) {
    null;
  } else {
    ret.add("}).call(this)");
  }
  return ret;
};
SPEC_DASH_OPS["do"] = sf_DASH_do;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-case] in file: src/czlab/kirby/cg/transpiler.ky near line: 513
const sf_DASH_case = function(ast, env) {
  let ret = node_QTE(tnode(), ast);
  let stmtQ = stmt_QUERY(ast);
  let brk = ";\nbreak;\n";
  let tst = ast[1];
  let _x = [gensym("C____")].join("");
  let e,
    t,
    c,
    dft;
  if ( (!(0 === (kirbystdlibref.count(ast) % 2))) ) {
    dft = ast.pop();
  }
  for (let i = 2, sz = kirbystdlibref.count(ast), ____break = false; ((!____break) && (i < sz)); i = (i + 2)) {
    (e = ast[i], c = ast[i + 1]);
    if (pairs_QUERY(e)) {
      for (let j = 0, sz = kirbystdlibref.count(e), ____break = false; ((!____break) && (j < sz)); j = (j + 1)) {
        ret.add(["case ", tx_STAR(e[j]), ":\n"]);
        if ( (j === (e.length - 1)) ) {
          ret.add([_x, "=", tx_STAR(c, env), brk]);
        }
      }
    } else {
      ret.add(["case ", tx_STAR(e), ":\n", _x, "=", tx_STAR(c, env), brk]);
    }
  }
  if (dft) {
    ret.add(["default:\n", _x, "=", tx_STAR(dft, env), brk]);
  }
  ret.prepend(["switch (", tx_STAR(tst, env), ") {\n"]);
  ret.add("}");
  if (stmtQ) {
    ret.prepend(["let ", _x, ";\n"]);
  } else {
    ret.prepend(["(function() { let ", _x, ";\n"]);
  }
  if (stmtQ) {
    null;
  } else {
    ret.add(["return ", _x, ";}).call(this)"]);
  }
  return ret;
};
SPEC_DASH_OPS["case"] = sf_DASH_case;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-vardefs] in file: src/czlab/kirby/cg/transpiler.ky near line: 553
const sf_DASH_vardefs = function(ast, env) {
  let ret = node_QTE(tnode(), ast);
  let cmd = [ast[0]].join("");
  let kks = {};
  let private_QUERY = cmd.endsWith("-");
  if ( (!("let" == cmd)) ) {
    cmd = "var";
  }
  for (let i = 1, s = null, sz = kirbystdlibref.count(ast), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    s = tx_STAR(ast[i]);
    ret.add(s);
    kks[s] = null;
  }
  ret.join(",");
  ret.prepend([cmd, " "].join(""));
  ret.add(";\n");
  if ( ((!private_QUERY) && (1 === rt.globalEnv().countNSPCache())) ) {
    let G__5 = kks;
    Object.entries(G__5).forEach(function(e) {
      return (function(v, k) {
        return EXTERNS[k] = k;
      })(e[1], e[0]);
    });
  }
  return ret;
};
SPEC_DASH_OPS["def~-"] = sf_DASH_vardefs;
SPEC_DASH_OPS["def~"] = sf_DASH_vardefs;
SPEC_DASH_OPS["var~"] = sf_DASH_vardefs;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-const] in file: src/czlab/kirby/cg/transpiler.ky near line: 578
const sf_DASH_const = function(ast, env) {
  let ret = node_QTE(tnode(), ast);
  let cmd = [ast[0]].join("");
  let keys = [];
  let vname = null;
  let private_QUERY = cmd.endsWith("-");
  let rval,
    rc,
    lhs,
    rhs;
  (ast = ast.slice(1), cmd = "const");
  for (let i = 0, sz = kirbystdlibref.count(ast), ____break = false; ((!____break) && (i < sz)); i = (i + 2)) {
    (lhs = ast[i], rhs = ast[i + 1], rval = tx_STAR(rhs, env));
    if (symbol_QUERY(lhs)) {
      lhs = tx_STAR(lhs);
      conj_BANG(keys, lhs);
      ret.add([cmd, " ", lhs, "= ", rval, ";\n"]);
    } else {
      out = tnode();
      rhs = destruct_STAR(lhs, out);
      ret.add(["let ", tx_STAR(rhs), "=", rval, ";\n"]);
      ret.add(out);
    }
  }
  if ( ((!private_QUERY) && (1 === rt.globalEnv().countNSPCache())) ) {
    keys.forEach(function() {
      let ____args = Array.prototype.slice.call(arguments);
      return EXTERNS[____args[0]] = ____args[0];
    });
  }
  return ret;
};
SPEC_DASH_OPS["const"] = sf_DASH_const;
SPEC_DASH_OPS["const-"] = sf_DASH_const;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-var] in file: src/czlab/kirby/cg/transpiler.ky near line: 609
const sf_DASH_var = function(ast, env) {
  let ret = node_QTE(tnode(), ast);
  let tmp = null;
  let vname = null;
  let cmd = [ast[0]].join("");
  let keys = [];
  let private_QUERY = (cmd !== "def");
  let rval,
    rc,
    lhs,
    rhs;
  (ast = ast.slice(1), cmd = ((cmd == "var") ?
    "let" :
    "var"));
  for (let i = 0, sz = kirbystdlibref.count(ast), ____break = false; ((!____break) && (i < sz)); i = (i + 2)) {
    (lhs = ast[i], rhs = ast[i + 1], rval = tx_STAR(rhs, env));
    if (symbol_QUERY(lhs)) {
      (lhs = tx_STAR(lhs), tmp = [cmd, " ", lhs, "= ", rval, ";\n"]);
      conj_BANG(keys, lhs);
      ret.add(tmp);
    } else {
      out = tnode();
      rhs = destruct_STAR(lhs, out);
      ret.add(["let ", tx_STAR(rhs), "=", rval, ";\n"]);
      ret.add(out);
    }
  }
  if ( ((!private_QUERY) && (1 === rt.globalEnv().countNSPCache())) ) {
    keys.forEach(function(k) {
      return EXTERNS[k] = k;
    });
  }
  return ret;
};
SPEC_DASH_OPS["def"] = sf_DASH_var;
SPEC_DASH_OPS["def-"] = sf_DASH_var;
SPEC_DASH_OPS["var"] = sf_DASH_var;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-inst?] in file: src/czlab/kirby/cg/transpiler.ky near line: 643
const sf_DASH_inst_QUERY = function(ast, env) {
  let ret = node_QTE(tnode(), ast);
  ret.add(["(", tx_STAR(ast[2], env), " instanceof ", tx_STAR(ast[1], env), ")"]);
  return ret;
};
SPEC_DASH_OPS["inst?"] = sf_DASH_inst_QUERY;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-delete] in file: src/czlab/kirby/cg/transpiler.ky near line: 653
const sf_DASH_delete = function(ast, env) {
  let ret = node_QTE(tnode(), ast);
  ret.add(["delete ", tx_STAR(ast[1], env)]);
  if ( (kirbystdlibref.count(ast) > 2) ) {
    ret.add(["[", tx_STAR(ast[2]), "]"]);
  }
  return ret;
};
SPEC_DASH_OPS["delete!"] = sf_DASH_delete;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-new] in file: src/czlab/kirby/cg/transpiler.ky near line: 662
const sf_DASH_new = function(ast, env) {
  let ret = node_QTE(tnode(), ast);
  ret.add(tx_STAR(ast.slice(1), env));
  ret.prepend("new ");
  return ret;
};
SPEC_DASH_OPS["new"] = sf_DASH_new;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-throw] in file: src/czlab/kirby/cg/transpiler.ky near line: 670
const sf_DASH_throw = function(ast, env) {
  let ret = node_QTE(tnode(), ast);
  let stmtQ = stmt_QUERY(ast);
  ret.add(["throw ", tx_STAR(ast[1], env)]);
  if ( (!stmtQ) ) {
    ret.prepend("(function (){ ");
    ret.add(";}).call(this)");
  }
  return ret;
};
SPEC_DASH_OPS["throw"] = sf_DASH_throw;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-x-opop] in file: src/czlab/kirby/cg/transpiler.ky near line: 681
const sf_DASH_x_DASH_opop = function(ast, env) {
  txExpr(ast, env);
  return node_QTE(tnodeEx([ast[0], ast[1]]), ast);
};
SPEC_DASH_OPS["++"] = sf_DASH_x_DASH_opop;
SPEC_DASH_OPS["--"] = sf_DASH_x_DASH_opop;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-x-eq] in file: src/czlab/kirby/cg/transpiler.ky near line: 688
const sf_DASH_x_DASH_eq = function(ast, env) {
  txExpr(ast, env);
  return node_QTE(tnodeEx([ast[1], " ", ast[0], " ", ast[2]]));
};
SPEC_DASH_OPS["+="] = sf_DASH_x_DASH_eq;
SPEC_DASH_OPS["-="] = sf_DASH_x_DASH_eq;
SPEC_DASH_OPS["*="] = sf_DASH_x_DASH_eq;
SPEC_DASH_OPS["/="] = sf_DASH_x_DASH_eq;
SPEC_DASH_OPS["%="] = sf_DASH_x_DASH_eq;
SPEC_DASH_OPS["<<="] = sf_DASH_x_DASH_eq;
SPEC_DASH_OPS[">>="] = sf_DASH_x_DASH_eq;
SPEC_DASH_OPS[">>>="] = sf_DASH_x_DASH_eq;
SPEC_DASH_OPS["&="] = sf_DASH_x_DASH_eq;
SPEC_DASH_OPS["|="] = sf_DASH_x_DASH_eq;
SPEC_DASH_OPS["^="] = sf_DASH_x_DASH_eq;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-set-in] in file: src/czlab/kirby/cg/transpiler.ky near line: 699
const sf_DASH_set_DASH_in = function(ast, env) {
  if ( (0 === (kirbystdlibref.count(ast) % 2)) ) {
    true;
  } else {
    throw new Error("set-in: bad arg count");
  }
  let ret = node_QTE(tnode(), ast);
  let more = false;
  let obj = tx_STAR(ast[1], env);
  for (let i = 2, sz = kirbystdlibref.count(ast), ____break = false; ((!____break) && (i < sz)); i = (i + 2)) {
    if ( (i > 2) ) {
      ret.add(",");
      more = true;
    }
    ret.add([obj, "[", tx_STAR(ast[i], env), "]", "=", tx_STAR(ast[i + 1], env)]);
  }
  if (more) {
    ret.prepend("(");
    ret.add(")");
  }
  return ret;
};
SPEC_DASH_OPS["set-in!"] = sf_DASH_set_DASH_in;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-set] in file: src/czlab/kirby/cg/transpiler.ky near line: 719
const sf_DASH_set = function(ast, env) {
  if ( (!(0 === (kirbystdlibref.count(ast) % 2))) ) {
    true;
  } else {
    throw new Error("set: bad arg count");
  }
  let more = false;
  let ret = node_QTE(tnode(), ast);
  for (let i = 1, sz = kirbystdlibref.count(ast), ____break = false; ((!____break) && (i < sz)); i = (i + 2)) {
    if ( (i > 1) ) {
      ret.add(",");
      more = true;
    }
    ret.add([tx_STAR(ast[i], env), "=", tx_STAR(ast[i + 1], env)]);
  }
  if (more) {
    ret.prepend("(");
    ret.add(")");
  }
  return ret;
};
SPEC_DASH_OPS["set!"] = sf_DASH_set;
/////////////////////////////////////////////////////////////////////////////////
//name: [parseFuncArgs] in file: src/czlab/kirby/cg/transpiler.ky near line: 738
const parseFuncArgs = function(args) {
  let pms = [];
  let ret = [];
  let e,
    ev;
  for (let pos = 0, sz = kirbystdlibref.count(args), ____break = false; ((!____break) && (pos < sz)); pos = (pos + 1)) {
    (e = args[pos], ev = [e].join(""));
    if (symbol_QUERY(e)) {
      if ( (ev === "&") ) {
        e = args[pos + 1];
        if (symbol_QUERY(e)) {
          ret.add(["let ", tx_STAR(e), "=slice(arguments,", pos, ");\n"]);
        } else {
          if (map_QUERY(e)) {
            destructMap();
          } else {
            if (Array.isArray(e)) {
              destructVec();
            } else {
              null;
            }
          }
        }
      } else {
        if (ev.startsWith("&")) {
          ret.add(["let ", rdr.jsid(ev.slice(1)), "=slice(arguments,", pos, ");\n"]);
        } else {
          if ( (ev == "_") ) {
            null;
          } else {
            if (true) {
              conj_BANG(pms, e);
            }
          }
        }
      }
    } else {
      if (map_QUERY(e)) {
        destructMap();
      } else {
        if (Array.isArray(e)) {
          destructVec();
        } else {
          if (true) {
            throw new Error(["bad function arg: ", typeid(e)].join(""));
          }
        }
      }
    }
  }
  return ret;
};
///////////////////////////////////////////////////////////////////////////////////
//name: [XXparseFuncArgs] in file: src/czlab/kirby/cg/transpiler.ky near line: 774
const XXparseFuncArgs = function(args) {
  let ret = [];
  let e,
    ev;
  for (let i = 0, sz = kirbystdlibref.count(args), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    (e = args[i], ev = [e].join(""));
    if (symbol_QUERY(e)) {
      if ( (ev === "_") ) {
        conj_BANG(ret, gensym("_"));
      } else {
        if ( (ev === "&") ) {
          e = args[i + 1];
          if (symbol_QUERY(e)) {
            conj_BANG(ret, [e, i, e]);
          } else {
            conj_BANG(ret, [symbol(["&", [gensym("A____")].join("")].join("")), i, e]);
          }
          ++i;
        } else {
          if (ev.startsWith("&")) {
            e = symbol(ev.slice(1));
            conj_BANG(ret, [e, i, e]);
          } else {
            if (true) {
              conj_BANG(ret, e);
            }
          }
        }
      }
    } else {
      if (keyword_QUERY(e)) {
        throw new Error(["bad function args destructure: ", typeid(e)].join(""));
      } else {
        if (Array.isArray(e)) {
          conj_BANG(ret, [gensym(), i, e]);
        } else {
          if (true) {
            throw new Error(["bad function args destructure: ", typeid(e)].join(""));
          }
        }
      }
    }
  }
  return ret;
};
//////////////////////////////////////////////////////////////////////////////////
//name: [handleFuncArgs] in file: src/czlab/kirby/cg/transpiler.ky near line: 804
const handleFuncArgs = function(fargs, env) {
  let out = [];
  let misc = [];
  let keys = [];
  let ret = tnode();
  let knode = tnode();
  fargs.forEach(function(arg) {
    return (symbol_QUERY(arg) ?
      conj_BANG(keys, arg) :
      ((symbol_QUERY(arg[0]) && (![arg[0]].join("").startsWith("&")) && Array.isArray(arg[2])) ?
        (function() {
          conj_BANG(keys, arg[0]);
          return conj_BANG(misc, arg);
        }).call(this) :
        (true ?
          conj_BANG(misc, arg) :
          null)));
  });
  misc.forEach(function(arr) {
    let a0 = arr[0];
    let name = [a0].join("");
    let varg_QUERY = name.startsWith("&");
    let pos = [arr[1]].join("");
    let a2 = arr[2];
    if (varg_QUERY) {
      name = name.slice(1);
    }
    name = rdr.jsid(name);
    return ((symbol_QUERY(a0) && symbol_QUERY(a2)) ?
      ret.add(["let ", name, "=Array.prototype.slice.call(arguments,", pos, ");\n"]) :
      (Array.isArray(a2) ?
        (function() {
          if (varg_QUERY) {
            ret.add(["let ", name, "=Array.prototype.slice.call(arguments,", pos, ");\n"]);
          }
          return ret.add(destruct0("let", a2, name, env)[0]);
        }).call(this) :
        null));
  });
  knode.add(keys.map(function() {
    let ____args = Array.prototype.slice.call(arguments);
    return rdr.jsid([____args[0]].join(""));
  }).join(","));
  return conj_BANG(out, knode, ret);
};
////////////////////////////////////////////////////////////////////////////////
//name: [sf-fn] in file: src/czlab/kirby/cg/transpiler.ky near line: 839
const sf_DASH_fn = function(ast, env) {
  let ret = node_QTE(tnode(), ast);
  let args = ast[1];
  let fargs = null;
  let hints = {};
  let body = ast.slice(2);
  if ( ((3 === kirbystdlibref.count(args)) && ("with-meta" == args[0]) && Array.isArray(args[1])) ) {
    (hints = evalMeta(args[2], env), args = args[1]);
  }
  fargs = processFuncArgs(args, env);
  ret.add(["function (", fargs[0], ") {\n", fargs[1], txDo(body, env, true), "}"]);
  return ret;
};
SPEC_DASH_OPS["fn"] = sf_DASH_fn;
//////////////////////////////////////////////////////////////////////////////////
//name: [fmtRegoSpecOps] in file: src/czlab/kirby/cg/transpiler.ky near line: 857
const fmtRegoSpecOps = function(alias, fname) {
  return [rdr.jsid("SPEC-OPS"), "[\"", alias, "\"] = ", fname].join("");
};
////////////////////////////////////////////////////////////////////////////////
//name: [fmtSpecOps] in file: src/czlab/kirby/cg/transpiler.ky near line: 863
const fmtSpecOps = function(fname, attrs) {
  let ks = (attrs["opcode"] || []);
  let out = ks.map(function() {
    let ____args = Array.prototype.slice.call(arguments);
    return fmtRegoSpecOps([____args[0]].join(""), fname);
  }).join(";\n");
  return ((!(0 === kirbystdlibref.count(ks))) ?
    [out, ";\n"].join("") :
    out);
};
/////////////////////////////////////////////////////////////////////////////////
//name: [writeFuncInfo] in file: src/czlab/kirby/cg/transpiler.ky near line: 871
const writeFuncInfo = function(fname, src) {
  let s = ["//name: [", fname, "] in file: ", ast.source, " near line: ", ast.line, "\n"].join("");
  let len = s.length;
  if ( (len < 80) ) {
    len = 80;
  }
  return (function() {
    let G__6 = tnodeEx(["/".repeat(len), "\n"].join(""));
    G__6.add(s);
    return G__6;
  }).call(this);
};
////////////////////////////////////////////////////////////////////////////////
//name: [sf-func] in file: src/czlab/kirby/cg/transpiler.ky near line: 880
const sf_DASH_func = function(ast, env) {
  let mtd_QUERY = (ast[0] == "method");
  let fname0 = [ast[0]].join("");
  let fname = tx_STAR(ast[1], env);
  let dot_QUERY = contains_QUERY(fname, ".");
  let ret = node_QTE(tnode(), ast);
  let hints = {};
  let fargs = null;
  let e2 = ast[2];
  let doc = null;
  let args = 2;
  let body = 3;
  let private_QUERY = [ast[0]].join("").endsWith("-");
  if ( (typeof (e2) === "string") ) {
    (doc = 2, args = 3);
  }
  body = (args + 1);
  if (doc) {
    doc = ast[doc];
  }
  (args = ast[args], body = ast.slice(body));
  if ( ((3 === kirbystdlibref.count(args)) && ("with-meta" == args[0]) && Array.isArray(args[1])) ) {
    (hints = evalMeta(args[2], env), args = args[1]);
  }
  fargs = processFuncArgs(args, env);
  if (mtd_QUERY) {
    if (hints.static) {
      ret.add("static ");
    }
    ret.add([fname, " ("]);
    if ( (fname == "constructor") ) {
      body.push(symbol("this"));
    }
  } else {
    if (dot_QUERY) {
      ret.add([fname, " = function ("]);
    } else {
      if (true) {
        ret.add(["const ", fname, " = function ("]);
      }
    }
  }
  ret.add([fargs[0], ") {\n", fargs[1], txDo(body, env, true), "};\n"]);
  if ( (!(0 === kirbystdlibref.count(hints))) ) {
    ret.add(fmtSpecOps(fname, hints));
  }
  if (doc) {
    ret.prepend(writeDoc(doc));
  }
  ret.prepend(writeFuncInfo(fname0, ast));
  if ( ((!private_QUERY) && (!dot_QUERY) && (!mtd_QUERY) && (1 === rt.globalEnv().countNSPCache())) ) {
    EXTERNS[fname] = fname;
  }
  return ret;
};
SPEC_DASH_OPS["defn"] = sf_DASH_func;
SPEC_DASH_OPS["defn-"] = sf_DASH_func;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-try] in file: src/czlab/kirby/cg/transpiler.ky near line: 934
const sf_DASH_try = function(ast, env) {
  let stmtQ = stmt_QUERY(ast);
  let sz = kirbystdlibref.count(ast);
  let ret = node_QTE(tnode(), ast);
  let t,
    f,
    c;
  f = last(ast);
  if ( (Array.isArray(f) && ("finally" == f[0])) ) {
    (f = ast.pop(), sz = kirbystdlibref.count(ast));
  } else {
    f = null;
  }
  c = null;
  if ( (sz > 1) ) {
    c = ast[(sz - 1)];
  }
  if ( (Array.isArray(c) && ("catch" == c[0])) ) {
    if ( ((kirbystdlibref.count(c) < 2) || (!symbol_QUERY(c[1]))) ) {
      syntax_BANG("e0", ast);
    }
    c = ast.pop();
  } else {
    c = null;
  }
  if ( ((f === null) && (c === null)) ) {
    syntax_BANG("e0", ast);
  }
  ret.add(["try {\n", txDo(exprHint(ast.slice(1), (!stmtQ)), env), "\n}"]);
  if (c) {
    t = c[1];
    ret.add([["catch (", t, ") {\n"].join(""), txDo(exprHint(c.slice(2), (!stmtQ)), env), ";\n}\n"]);
  }
  if (f) {
    ret.add(["finally {\n", txDo(exprHint(f.slice(1), false), env, false), ";\n}\n"]);
  }
  if ( (!stmtQ) ) {
    ret.prepend("(function(){\n");
    ret.add("}).call(this)");
  }
  return ret;
};
SPEC_DASH_OPS["try"] = sf_DASH_try;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-if] in file: src/czlab/kirby/cg/transpiler.ky near line: 976
const sf_DASH_if = function(ast, env) {
  let ret = node_QTE(tnode(), ast);
  let stmtQ = stmt_QUERY(ast);
  let a1 = exprHint(ast[1], (!stmtQ));
  let a2 = exprHint(ast[2], (!stmtQ));
  let a3 = ((kirbystdlibref.count(ast) > 3) ?
    ast[3] :
    null);
  let elze = (a3 ?
    exprHint(a3, (!stmtQ)) :
    null);
  (a1 = tx_STAR(a1, env), a2 = tx_STAR(a2, env), elze = tx_STAR(elze, env));
  if (stmtQ) {
    ret.add(["if (", a1, ") {\n", a2, ";\n}"]);
    if (a3) {
      ret.add([" else { \n", elze, ";\n}"]);
    }
  } else {
    ret.add(["(", a1, " ?\n", a2, " :\n",(elze || "null"), ")"]);
  }
  return ret;
};
SPEC_DASH_OPS["if"] = sf_DASH_if;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-nth] in file: src/czlab/kirby/cg/transpiler.ky near line: 995
const sf_DASH_nth = function(ast, env) {
  let ret = node_QTE(tnode(), ast);
  txExpr(ast, env);
  ret.add([ast[1], "[", ast[2], "]"]);
  return ret;
};
SPEC_DASH_OPS["nth"] = sf_DASH_nth;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-get] in file: src/czlab/kirby/cg/transpiler.ky near line: 1004
const sf_DASH_get = function(ast, env) {
  let ret = node_QTE(tnode(), ast);
  txExpr(ast, env);
  ret.add([ast[1], "[", ast[2], "]"]);
  return ret;
};
SPEC_DASH_OPS["aget"] = sf_DASH_get;
SPEC_DASH_OPS["get"] = sf_DASH_get;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-array] in file: src/czlab/kirby/cg/transpiler.ky near line: 1013
const sf_DASH_array = function(ast, env) {
  let ret = node_QTE(tnode(), ast);
  if ( (0 === kirbystdlibref.count(ast)) ) {
    ret.add("[]");
  } else {
    if ( (!vector_QUERY(ast)) ) {
      ast.splice(0, 1);
    }
    txExpr(ast, env);
    ret.add("[\n");
    for (let i = 0, sz = kirbystdlibref.count(ast), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
      if ( (i > 0) ) {
        ret.add(",\n");
      }
      ret.add(ast[i]);
    }
    ret.add("\n]");
  }
  return ret;
};
SPEC_DASH_OPS["vec"] = sf_DASH_array;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-object] in file: src/czlab/kirby/cg/transpiler.ky near line: 1031
const sf_DASH_object = function(ast, env) {
  let ret = node_QTE(tnode(), ast);
  if ( (0 === kirbystdlibref.count(ast)) ) {
    ret.add("{}");
  } else {
    if ( (!map_QUERY(ast)) ) {
      ast.splice(0, 1);
    }
    txExpr(ast, env);
    ret.add("{\n");
    for (let i = 0, sz = kirbystdlibref.count(ast), ____break = false; ((!____break) && (i < sz)); i = (i + 2)) {
      if ( (i > 0) ) {
        ret.add(",\n");
      }
      ret.add([ast[i], ": ", ast[i + 1]]);
    }
    ret.add("\n}");
  }
  return ret;
};
SPEC_DASH_OPS["hashmap"] = sf_DASH_object;
var icache = [];
////////////////////////////////////////////////////////////////////////////////
//name: [includeFile] in file: src/czlab/kirby/cg/transpiler.ky near line: 1052
const includeFile = function(fname) {
  return (contains_QUERY(icache, fname) ?
    tnode() :
    (function() {
      let src = fs.readFileSync(fname, "utf-8");
      conj_BANG(icache, fname);
      return txTree(psr.parser(src, fname), rt.globalEnv());
    }).call(this));
};
////////////////////////////////////////////////////////////////////////////////
//name: [sf-include] in file: src/czlab/kirby/cg/transpiler.ky near line: 1061
const sf_DASH_include = function(ast, env) {
  let ret = [];
  let dir = path.dirname(ast.source);
  let fname,
    e;
  for (let i = 1, sz = kirbystdlibref.count(ast), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    e = ast[i];
    if ( ((!Array.isArray(e)) || (1 !== kirbystdlibref.count(e))) ) {
      syntax_BANG("e0", ast);
    }
    fname = e[0];
    if (fname) {
      fname = fname.replace(/[\"]/g, "");
    }
    try {
      fname = fs.realpathSync([dir, "/", fname].join(""));
    } catch (e) {
      syntax_BANG("e11", ast);
    }
    try {
      conj_BANG(ret, includeFile(fname));
    } finally {
      rt.globalEnv().popNSP();
    }
  }
  return ((0 === kirbystdlibref.count(ret)) ?
    tnode() :
    ret);
};
////////////////////////////////////////////////////////////////////////////////
//name: [sf-require] in file: src/czlab/kirby/cg/transpiler.ky near line: 1085
const sf_DASH_require = function(ast, env) {
  let as,
    path,
    v,
    e,
    refers,
    renames;
  let ret = node_QTE(tnode(), ast);
  for (let i = 1, sz = kirbystdlibref.count(ast), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    (e = ast[i], as = [gensym("R____")].join(""), refers = null, renames = null);
    if ( ((!Array.isArray(e)) || (kirbystdlibref.count(e) < 3)) ) {
      syntax_BANG("e0", ast);
    }
    path = e[0];
    for (let j = 1, sz = kirbystdlibref.count(e), ____break = false; ((!____break) && (j < sz)); j = (j + 1)) {
      v = e[j];
      if ( (v == "as") ) {
        as = [e[j + 1]].join("");
        ++j;
      } else {
        if ( (v == "refer") ) {
          refers = e[j + 1];
          ++j;
        } else {
          if ( (v == "rename") ) {
            renames = e[j + 1];
            ++j;
          } else {
            null;
          }
        }
      }
    }
    ret.add(["const ", rdr.jsid(as), "= require(", tx_STAR(path), ");\n"]);
    for (let i = 0, sz = kirbystdlibref.count(refers), ____break = false; ((!____break) && (refers && (i < sz))); i = (i + 1)) {
      v = tx_STAR(refers[i]);
      ret.add(["const ", v, "=", as, "[\"", v, "\"];\n"]);
    }
    for (let i = 0, sz = kirbystdlibref.count(renames), ____break = false; ((!____break) && (renames && (i < sz))); i = (i + 2)) {
      (e = tx_STAR(renames[i]), v = tx_STAR(renames[i + 1]));
      ret.add(["const ", v, "=", as, "[\"", e, "\"];\n"]);
    }
  }
  return ret;
};
////////////////////////////////////////////////////////////////////////////////
//name: [evalMeta] in file: src/czlab/kirby/cg/transpiler.ky near line: 1126
const evalMeta = function(ast, env) {
  return (Array.isArray(ast) ?
    JSON.parse([tx_STAR(ast, env)].join("")) :
    (keyword_QUERY(ast) ?
      JSON.parse([tx_STAR([ast, true], env)].join("")) :
      (symbol_QUERY(ast) ?
        JSON.parse([tx_STAR([symbol("tag"), ast], env)].join("")) :
        (true ?
          (function() {
            throw new Error(["Bad meta value", prn(ast)].join("")) ;
          }).call(this) :
          null))));
};
////////////////////////////////////////////////////////////////////////////////
//name: [sf-ns] in file: src/czlab/kirby/cg/transpiler.ky near line: 1142
const sf_DASH_ns = function(ast, env) {
  let ret = [];
  let e = null;
  let hints = {};
  let nsp = ast[1];
  if ( (Array.isArray(nsp) && (3 === kirbystdlibref.count(nsp)) && ("with-meta" == nsp[0]) && symbol_QUERY(nsp[1])) ) {
    (hints = evalMeta(nsp[2], env), nsp = nsp[1]);
  }
  rt.globalEnv().pushNSP([nsp].join(""));
  ast = ast.slice(2);
  for (let i = 0, sz = kirbystdlibref.count(ast), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    e = ast[i];
    if ( (pairs_QUERY(e) && ("include" == e[0])) ) {
      conj_BANG(ret, sf_DASH_include(e));
    } else {
      if ( (pairs_QUERY(e) && ("require" == e[0])) ) {
        conj_BANG(ret, sf_DASH_require(e));
      } else {
        null;
      }
    }
  }
  nsp = rt.globalEnv().peekNSP();
  if ( ((nsp === "czlab.kirby.bl.macros") || (nsp === "czlab.kirby.bl.stdlib")) ) {
    null;
  } else {
    if (nsp.startsWith("czlab.kirby.")) {
      conj_BANG(ret, tnodeEx("const kirbystdlibref=std;\n"));
    } else {
      if (true) {
        conj_BANG(ret, sf_DASH_require([kirbystdlibref.symbol("require"), ["kirby", kirbystdlibref.keyword(":as"), kirbystdlibref.symbol("kirbystdlibref")]]));
      }
    }
  }
  return ret;
};
SPEC_DASH_OPS["ns"] = sf_DASH_ns;
/////////////////////////////////////////////////////////////////////////////////////
//name: [maybeStripStdlib] in file: src/czlab/kirby/cg/transpiler.ky near line: 1178
const maybeStripStdlib = function(cmd) {
  let lib = "kirbystdlibref.";
  let nsp = rt.globalEnv().peekNSP();
  cmd = [cmd].join("");
  return ((cmd.startsWith(lib) && (nsp === "czlab.kirby.bl.stdlib")) ?
    cmd.slice(kirbystdlibref.count(lib)) :
    cmd);
};
////////////////////////////////////////////////////////////////////////////////
//name: [sf-comment] in file: src/czlab/kirby/cg/transpiler.ky near line: 1188
const sf_DASH_comment = function(ast, env) {
  return node_QTE(tnode(), ast);
};
SPEC_DASH_OPS["comment"] = sf_DASH_comment;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-for] in file: src/czlab/kirby/cg/transpiler.ky near line: 1193
const sf_DASH_for = function(ast, env) {
  let ret = node_QTE(tnode(), ast);
  let stmtQ = stmt_QUERY(ast);
  let body = exprHint(ast.slice(2), false);
  if ( (0 === kirbystdlibref.count(body)) ) {
    tnode();
  } else {
    sf_DASH_floop(ret, ast[1], body, env, stmtQ);
  }
  return ret;
};
SPEC_DASH_OPS["for"] = sf_DASH_for;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-floop] in file: src/czlab/kirby/cg/transpiler.ky near line: 1204
const sf_DASH_floop = function(ret, args, body, env, stmtQ) {
  let vars = [];
  let recurs = [];
  let tst,
    nb,
    e;
  for (let i = 0, sz = kirbystdlibref.count(args), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    e = args[i];
    if ( (e == "while") ) {
      tst = args[i + 1];
      ++i;
    } else {
      if ( (e == "recur") ) {
        recurs = args.slice((i + 1));
        ____break = true;
      } else {
        if (symbol_QUERY(e)) {
          conj_BANG(vars, e, args[i + 1]);
          ++i;
        } else {
          null;
        }
      }
    }
  }
  ret.add("for (");
  for (let i = 0, sz = kirbystdlibref.count(vars), ____break = false; ((!____break) && (i < sz)); i = (i + 2)) {
    if ( (i === 0) ) {
      ret.add("let ");
    }
    if ( (i !== 0) ) {
      ret.add(",");
    }
    ret.add([tx_STAR(vars[i]), "=", tx_STAR(vars[i + 1], env)]);
  }
  if ( (0 === kirbystdlibref.count(vars)) ) {
    ret.add("let ____break=false;");
  } else {
    ret.add(",____break=false;");
  }
  nb = [kirbystdlibref.symbol("not"), kirbystdlibref.symbol("____break")];
  if (tst) {
    tst = [symbol("and"), nb, tst];
  } else {
    tst = nb;
  }
  ret.add(tx_STAR(tst, env));
  ret.add("; ");
  for (let i = 0, k = 0, sz = kirbystdlibref.count(recurs), ____break = false; ((!____break) && (i < sz)); i = (i + 1), k = (k + 2)) {
    if ( (i !== 0) ) {
      ret.add(",");
    }
    ret.add([tx_STAR(vars[k]), "=", tx_STAR(recurs[i], env)]);
  }
  ret.add("){\n");
  ret.add(txDo(body, env, false));
  ret.add("}\n");
  if ( (!stmtQ) ) {
    ret.prepend("(function() {\n");
    ret.add("}).call(this)");
  }
  return ret;
};
////////////////////////////////////////////////////////////////////////////////
//name: [sf-jscode] in file: src/czlab/kirby/cg/transpiler.ky near line: 1248
const sf_DASH_jscode = function(ast, env) {
  return node_QTE(tnodeEx([ast[1]].join("").replace(rdr.REGEX.dquoteHat, "").replace(rdr.REGEX.dquoteEnd, "")), ast);
};
SPEC_DASH_OPS["js#"] = sf_DASH_jscode;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-macro] in file: src/czlab/kirby/cg/transpiler.ky near line: 1255
const sf_DASH_macro = function(ast, env) {
  let pms = [];
  let args = ast[2];
  let body = ast.slice(3);
  let x,
    e,
    ev;
  if ( (typeof (args) === "string") ) {
    (doc = args, args = ast[3], body = ast.slice(4));
  }
  for (let i = 0, sz = kirbystdlibref.count(args), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    (e = args[i], ev = [e].join(""));
    if ( (ev === "&") ) {
      if (Array.isArray(args[i + 1])) {
        e = args[i + 1];
        ++i;
        for (let j = 0, sz = kirbystdlibref.count(e), ____break = false; ((!____break) && (j < sz)); j = (j + 1)) {
          x = e[j];
          if ( (!symbol_QUERY(x)) ) {
            throw new Error("Bad optional arg for macro");
          }
          conj_BANG(pms, x);
        }
      } else {
        conj_BANG(pms, e, args[i + 1]);
        ++i;
      }
    } else {
      if ( (!symbol_QUERY(e)) ) {
        throw new Error("Bad optional arg for macro");
      } else {
        if (true) {
          conj_BANG(pms, e);
        }
      }
    }
  }
  ast = [ast[0], ast[1], [symbol("fn*"), pms].concat(body)];
  let a1 = [ast[1]].join("");
  let a2 = ast[2];
  let func = rt.compute(a2, env);
  func["____macro"] = true;
  if (doc) {
    func["____doc"] = doc;
  }
  rt.setMacro(a1, func);
  return node_QTE(tnode(), ast);
};
SPEC_DASH_OPS["defmacro"] = sf_DASH_macro;
////////////////////////////////////////////////////////////////////////////////
//name: [sf-unary] in file: src/czlab/kirby/cg/transpiler.ky near line: 1299
const sf_DASH_unary = function(ast, env) {
  let ret = node_QTE(tnode(), ast);
  let G____13 = ast;
  let a0 = G____13[0];
  let a1 = G____13[1];
  if ( (a0 == "not") ) {
    a0 = symbol("!");
  }
  ret.add(["(", tx_STAR(a0, env), tx_STAR(a1, env), ")"]);
  return ret;
};
SPEC_DASH_OPS["not"] = sf_DASH_unary;
SPEC_DASH_OPS["~"] = sf_DASH_unary;
SPEC_DASH_OPS["!"] = sf_DASH_unary;
////////////////////////////////////////////////////////////////////////////////
//name: [spitExterns] in file: src/czlab/kirby/cg/transpiler.ky near line: 1309
const spitExterns = function() {
  let s = "";
  let ks = Object.keys(EXTERNS);
  if ( (!(0 === kirbystdlibref.count(ks))) ) {
    (s = ks.map(function() {
      let ____args = Array.prototype.slice.call(arguments);
      return [____args[0], ": ", ____args[0]].join("");
    }).join(",\n"), s = ["\n\nmodule.exports = {\n", s, "\n};\n\n"].join(""));
  }
  return s;
};
////////////////////////////////////////////////////////////////////////////////
//name: [banner] in file: src/czlab/kirby/cg/transpiler.ky near line: 1318
const banner = function() {
  return ["/*", "Auto generated by Kirby - v", MODULE_DASH_VERSION, " ", rt.globalEnv().firstNSP(), " - ", new Date(), "*/\n\n"].join("");
};
////////////////////////////////////////////////////////////////////////////////
//name: [cleanCode] in file: src/czlab/kirby/cg/transpiler.ky near line: 1325
const cleanCode = function(code) {
  let tmp = null;
  let arr = [];
  let G__7 = code.split("\n");
  for (let G__9 = 0, G__8 = false, ____break = false; ((!____break) && ((!G__8) && (G__9 < G__7.length))); G__9 = (G__9 + 1)) {
    let s = G__7[G__9];
    null;
    if ( (!true) ) {
      G__8 = true;
    }
    if ( ((!G__8) && true) ) {
      tmp = s.trim();
      if ( ((!(0 === kirbystdlibref.count(tmp))) && (tmp !== ";")) ) {
        conj_BANG(arr, s);
      }
    }
  }
  null;
  return arr.join("\n");
};
//////////////////////////////////////////////////////////////////////////////////
//name: [transpileCode] in file: src/czlab/kirby/cg/transpiler.ky near line: 1334
const transpileCode = function(codeStr, fname, options) {
  rt.globalEnv().resetNSPCache();
  EXTERNS = {};
  let outNode = txTree(psr.parser(codeStr, fname), rt.globalEnv());
  let G____14 = options;
  let source_DASH_map = G____14["source-map"];
  let format = G____14["format"];
  let cstr = "";
  let fopts = {};
  let extra = spitExterns();
  format = true;
  cstr = (source_DASH_map ?
    (function() {
      let outFile = [path.basename(fname, ".ky"), ".js"].join("");
      let srcMap = [outFile, ".map"].join("");
      let output = outNode.toStringWithSourceMap({
        "file": outFile
      });
      fs.writeFileSync(srcMap, output.map);
      return [output.code, extra, "\n//# sourceMappingURL=", path.relative(path.dirname(fname), srcMap)].join("");
    }).call(this) :
    [outNode, extra].join(""));
  if (format) {
    cstr = esfmt.format(cstr, fopts);
  }
  cstr = cleanCode(cstr);
  return ((0 === kirbystdlibref.count(cstr)) ?
    "" :
    [banner(), cstr].join(""));
};
/////////////////////////////////////////////////////////////////////////////////
//name: [transpileXXX] in file: src/czlab/kirby/cg/transpiler.ky near line: 1362
const transpileXXX = function(code, file, options) {
  return (function() {
    try {
      return transpileCode(code, file, options);
    } catch (e) {
      return (e ?
        (function() {
          if (console) {
            console.log([e.stack].join(""));
          }
          return (function() {
            throw e ;
          }).call(this);
        }).call(this) :
        null);
    }
  }).call(this);
};
////////////////////////////////////////////////////////////////////////////////////////
//name: [transpileWithSrcMap] in file: src/czlab/kirby/cg/transpiler.ky near line: 1370
const transpileWithSrcMap = function(code, file) {
  let G____15 = Array.prototype.slice.call(arguments, 2);
  let options = G____15[0];
  return transpileXXX(code, file, opt_QUERY__QUERY(options, {}));
};
////////////////////////////////////////////////////////////////////////////////
//name: [transpile] in file: src/czlab/kirby/cg/transpiler.ky near line: 1377
const transpile = function(code, file) {
  let G____16 = Array.prototype.slice.call(arguments, 2);
  let options = G____16[0];
  return transpileXXX(code, file, opt_QUERY__QUERY(options, {}));
};
///////////////////////////////////////////////////////////////////////////////////////
//name: [parseWithSourceMap] in file: src/czlab/kirby/cg/transpiler.ky near line: 1384
const parseWithSourceMap = function(codeStr, fname) {
  let outNode = txTree(psr.parser(codeStr, fname));
  outNode.prepend(banner());
  return outNode.toStringWithSourceMap();
};
var version = MODULE_DASH_VERSION;
////////////////////////////////////////////////////////////////////////////////////
//name: [processFuncArgs] in file: src/czlab/kirby/cg/transpiler.ky near line: 1394
const processFuncArgs = function(args, env) {
  let pms = [];
  let fdefs = tnode();
  let fargs = tnode();
  let rhs,
    e,
    ev,
    rval,
    out;
  for (let i = 0, sz = count(args), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    e = args[i];
    if (symbol_QUERY(e)) {
      if ( (e == "&") ) {
        rval = tnode();
        rval.add(["Array.prototype.slice.call(arguments,", [i].join(""), ")"]);
        e = args[i + 1];
        if (symbol_QUERY(e)) {
          rhs = e;
          fdefs.add(["let ", tx_STAR(rhs), "=", rval, ";\n"]);
        } else {
          out = tnode();
          rhs = destruct_STAR(e, out);
          fdefs.add(["let ", tx_STAR(rhs), "=", rval, ";\n"]);
          fdefs.add(out);
        }
        ____break = true;
      } else {
        conj_BANG(pms, e);
      }
    } else {
      if (Array.isArray(e)) {
        rval = tnode();
        rval.add(["arguments[", [i].join(""), "]"]);
        out = tnode();
        rhs = destruct_STAR(e, out);
        conj_BANG(pms, rhs);
        fdefs.add(["let ", tx_STAR(rhs), "=", rval, ";\n"]);
        fdefs.add(out);
      } else {
        if (true) {
          throw new Error("bad");
        }
      }
    }
  }
  for (let i = 0, sz = count(pms), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    fargs.add(tx_STAR(pms[i]));
  }
  fargs.join(",");
  return [fargs, fdefs];
};
////////////////////////////////////////////////////////////////////////////////
//name: [destruct*] in file: src/czlab/kirby/cg/transpiler.ky near line: 1432
const destruct_STAR = function(coll, out) {
  let rhs = gensym();
  let e = null;
  for (let i = 0, sz = count(coll), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    e = coll[i];
    if ( (keyword_QUERY(e) && (e == "as")) ) {
      rhs = coll[i + 1];
    } else {
      ____break = true;
    }
  }
  if (map_QUERY(coll)) {
    out.add(destruct_DASH_Map(rhs, coll));
  } else {
    if (vector_QUERY(coll)) {
      out.add(destruct_DASH_Vec(rhs, coll));
    } else {
      null;
    }
  }
  return rhs;
};
/////////////////////////////////////////////////////////////////////////////////
//name: [destruct-Vec] in file: src/czlab/kirby/cg/transpiler.ky near line: 1449
const destruct_DASH_Vec = function(src, coll) {
  let e,
    rhs,
    rval,
    out;
  let ret = tnode();
  src = tx_STAR(src);
  for (let i = 0, sz = count(coll), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    e = coll[i];
    if (symbol_QUERY(e)) {
      if ( (e == "_") ) {
        null;
      } else {
        if ( (e == "&") ) {
          rval = tnode();
          rval.add(["Array.prototype.slice.call(", src, ",", [i].join(""), ")"]);
          out = tnode();
          e = coll[i + 1];
          if (symbol_QUERY(e)) {
            rhs = e;
          } else {
            rhs = destruct_STAR(e, out);
          }
          ret.add(["let ", tx_STAR(rhs), "=", rval, ";\n"]);
          ret.add(out);
          ____break = true;
        } else {
          if (true) {
            out = tnode();
            out.add(["let ", tx_STAR(e), "=", src, "[", [i].join(""), "];\n"]);
            ret.add(out);
          }
        }
      }
    } else {
      if (Array.isArray(e)) {
        rval = tnode();
        rval.add([src, "[", [i].join(""), "]"]);
        out = tnode();
        rhs = destruct_STAR(e, out);
        ret.add(["let ", tx_STAR(rhs), "=", rval, ";\n"]);
        ret.add(out);
      } else {
        if (keyword_QUERY(e)) {
          if ( (e == "as") ) {
            ++i;
          } else {
            throw new Error("Bad keyword");
          }
        } else {
          if (true) {
            throw new Error("");
          }
        }
      }
    }
  }
  return ret;
};
/////////////////////////////////////////////////////////////////////////////////
//name: [destruct-Map] in file: src/czlab/kirby/cg/transpiler.ky near line: 1492
const destruct_DASH_Map = function(src, coll) {
  let ret = tnode();
  let e,
    arr;
  src = tx_STAR(src);
  for (let i = 0, sz = count(coll), ____break = false; ((!____break) && (i < sz)); i = (i + 1)) {
    e = coll[i];
    if (keyword_QUERY(e)) {
      if ( ((e == "keys") || (e == "strs")) ) {
        let arr = coll[i + 1];
        ++i;
        for (let j = 0, sz = count(arr), ____break = false; ((!____break) && (j < sz)); j = (j + 1)) {
          e = arr[j];
          ret.add(["let ", tx_STAR(e), "=", src, "[\"", [e].join(""), "\"];\n"]);
        }
      } else {
        if (true) {
          throw new Error(["bad keyword", e].join(""));
        }
      }
    } else {
      if (true) {
        throw new Error(["bad destruction", e].join(""));
      }
    }
  }
  return ret;
};
module.exports = {
  transpileWithSrcMap: transpileWithSrcMap,
  transpile: transpile,
  parseWithSourceMap: parseWithSourceMap,
  version: version
};