/*Auto generated by Kirby v1.0.0 - Sun Jan 21 2018 16:59:21 GMT-0800 (PST)
  czlab.kirby.main
({"doc":"","author":"Kenneth Leung"})
*/

const getopt = require("node-getopt");
const cp = require("child_process");
const watch = require("watch");
const path = require("path");
const fs = require("fs");
const R__6 = require("./macros");
const k = require("./kernel");
const std = require("./stdlib");
const object_QUERY = std["object_QUERY"];
const println = std["println"];
const rt = require("./engine");
const tx = require("./compiler");
const kirbystdlibref = std;
////////////////////////////////////////////////////////////////////////////////
//fn: [error!] in file: main.ky, line: 25
const error_BANG = function(msg) {
  println(msg);
  return process.exit(1);
};
////////////////////////////////////////////////////////////////////////////////
//fn: [compileSource] in file: main.ky, line: 28
const compileSource = function(opt) {
  let GS__11 = opt.argv;
  let fin = GS__11[0];
  let fout = GS__11[1];
  let options = opt.options;
  if ( (!fin) ) {
    error_BANG("No source file");
  } else {
    fin = path.resolve(fin);
  }
  if ( (!fin.endsWith(".ky")) ) {
    error_BANG("Source file extension not '.ky'");
  } else {
    null;
  }
  if ( (!fout) ) {
    fout = fin.replace(/\.ky$/g, ".js");
  } else {
    null;
  }
  return (function() {
    try {
      let GS__12 = options;
      let source_DASH_map = GS__12["source-map"];
      let format = GS__12["format"];
      let show_DASH_ast = GS__12["show-ast"];
      if ( (!show_DASH_ast) ) {
        println(["kirby v", tx.version].join(""), ": compiling: ", fin, " -> ", fout);
      } else {
        null;
      }
      let source = rt.slurp(fin);
      if (show_DASH_ast) {
        println(tx.dbgAST(source, fin));
      } else {
        let GS__13 = tx.transpile(source, fin, options);
        let ret = GS__13[0];
        let err = GS__13[1];
        rt.spit(fout, ret);
        if (err) {
          throw err;
        }
      }
      return null;
    } catch (e) {
      return error_BANG(e);
    }
  }).call(this);
};
////////////////////////////////////////////////////////////////////////////////
//fn: [init] in file: main.ky, line: 56
const init = function() {
  let c = require("./compiler");
  let f = function(module, fname) {
    let fin = path.resolve(fname);
    let GS__14 = c.transpile(rt.slurp(fin), fin);
    let code = GS__14[0];
    let err = GS__14[1];
    return (err ?
      (function() {
        throw err;
      }).call(this) :
      module._compile(code, fin));
  };
  require.extensions[".ky"] = f;
  return rt.init(tx.version);
};
////////////////////////////////////////////////////////////////////////////////
//fn: [doWatch] in file: main.ky, line: 68
const doWatch = function(cwd) {
  std.println("Watching", cwd, "for file changes...");
  return watch.watchTree(cwd, {
    "ignoreDirectoryPattern": /node_modules/,
    "ignoreDotFiles": true,
    "filter": function(f, stat) {
      return (f.endsWith(".ky") || stat.isDirectory());
    }
  }, function(f, curr, prev) {
    return ((object_QUERY(f) && ((prev === null) && (curr === null))) ?
      (function() {
        let GS__15 = null;
        "finished walking the tree";
        return GS__15;
      }).call(this) :
      ((curr && ((curr.nlink === 0))) ?
        (function() {
          let GS__16 = null;
          "f was removed";
          return GS__16;
        }).call(this) :
        (true ?
          (function() {
            return cp.spawn("bin/boot.js", [f.slice((cwd.length + 1))], {
              "stdio": "inherit"
            });
          }).call(this) :
          null)));
  });
};
////////////////////////////////////////////////////////////////////////////////
//fn: [pcli] in file: main.ky, line: 93
const pcli = function(gopt) {
  let opt = gopt.parseSystem();
  let GS__17 = opt.options;
  let version = GS__17["version"];
  let repl = GS__17["repl"];
  let watch = GS__17["watch"];
  let help = GS__17["help"];
  if (version) {
    console.info(tx.version);
  } else {
    if (watch) {
      doWatch(process.cwd());
    } else {
      if (repl) {
        rt.runRepl();
      } else {
        if ( (help || (0 === kirbystdlibref.count(opt.argv))) ) {
          gopt.showHelp();
        } else {
          if (true) {
            compileSource(opt);
          }
        }
      }
    }
  }
  return true;
};
////////////////////////////////////////////////////////////////////////////////
//fn: [main] in file: main.ky, line: 107
const main = function() {
  let gopt = getopt.create([["v", "verbose", "show details of the source"], ["w", "watch", "auto-compile changed files"], ["m", "source-map", "generate source maps"], ["f", "format", "format source code"], ["V", "version", "show version"], ["r", "repl", "start a repl"], ["h", "help", "show help"], ["t", "show-ast", "show AST"]]).setHelp(["kirby [OPTIONS] [<infile>] [<outfile>]\n\n", "<outfile> defaults to <infile>.js\n\n", "[[OPTIONS]]\n\n"].join("")).bindHelp();
  return (init() && pcli(gopt));
};
main();
module.exports = {
  da57bc0172fb42438a11e6e8778f36fb: {
    ns: "czlab.kirby.main",
    macros: {}
  },
  main: main
};